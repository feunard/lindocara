{
  "name": "@lindocara/lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:client": "vite build --outDir dist-client --emptyOutDir",
    "build": "yarn build:client && alepha build",
    "deploy": "yarn build:client && alepha platform up -e production",
    "preview": "vite preview",
    "test": "vitest run",
    "build:map": "tsx scripts/build-map.ts"
  },
  "dependencies": {
    "@lindocara/audio": "*",
    "@lindocara/engine": "*",
    "@lindocara/hd2d": "*",
    "alepha": "*",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
