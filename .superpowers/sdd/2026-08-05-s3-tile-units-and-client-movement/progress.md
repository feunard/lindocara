# SDD ledger — plan: docs/superpowers/plans/2026-08-05-s3-tile-units-and-client-movement.md
Task 1: implemented (commit 9be00b42), review: NEEDS FIXES — 1 Important
  (1) breakage list (a deliverable Task 7 uses as its checklist) missed packages/renderer/test/minimap.test.ts — minimap.ts:13 derives MINIMAP_WORLD_RADIUS from PLAYER_VISIBILITY_RADIUS and uses it against pixel points. Report also claimed "no other suite" without checking. Report-only fix dispatched.
Task 1: expected red (do NOT fix before Task 7): server rogue-interest-system.test.ts (1), world-room-combat.test.ts (1), renderer minimap.test.ts (1+)
Task 1: CARRY FORWARD to Task 6/7 — SPATIAL_CELL_SIZE and SPATIAL_EVENT_RADIUS are still PIXELS in interest.ts, deliberately (call sites in world-runtime.ts:507, worldState.ts:313, worldTick.ts:788 still use pixel positions). interest.ts is a mixed-unit file until then.
Task 1: CARRY FORWARD — packages/renderer is NOT in the plan's Task 7 file list but consumes converted constants (minimap.ts). Task 7 must cover it or say why not.
Task 1: fix round 1/5 dispatched (resumed implementer ae51feb2b4ee7faa7)
Task 1: fix round 1/5 — report-only fix; .superpowers/ is gitignored so there was no diff to re-review. CONTROLLER ADJUDICATION: verified directly instead — ran npm run test:renderer (1 failed / 177 passed, matching the report's named test exactly) and read the corrected breakage section. Finding ADDRESSED; overbroad "no other suite" claim withdrawn in the report.
Task 1: complete (commit 9be00b42, review clean after fix)
Task 1: minor (deferred): SPATIAL_EVENT_RADIUS/SPATIAL_CELL_SIZE have no per-constant "pixels" comment (header names them); the ordering test passes pre- and post-conversion so it is weak regression cover on its own
Task 2: implemented (commit 454e6ff4) — NOT YET REVIEWED. Resume point: generate review package (base 9be00b42) and dispatch the task reviewer.
Task 2: branch is DELIBERATELY not typechecking — 200 errors / 34 files (worldTick.ts 53, monster-system.ts 25, monster-system.test.ts 14, world-room-combat.test.ts 13). Verbatim list in task-2-report.md. Tasks 3-6 close it; Task 8 is the gate. Server suite 6 files / 27 tests red (baseline 2/2).
Task 2: CRITICAL CARRY-FORWARD — the compiler list is NECESSARY BUT NOT SUFFICIENT. 269 `.y` reads in packages/server/src still compile because they are structurally valid. A clean typecheck will NOT mean Phase A is done.
Task 2: SILENT SITE (worst one, Task 6 owns it) — packages/server/src/spatial-grid.ts: `SpatialEntity extends Vec2`, so every three-axis entity still satisfies it and the grid now indexes by (ground x, ELEVATION y). Zero compiler errors. Task 6 must convert SpatialEntity to {x, z} FIRST so it generates its own checklist.
Task 2: other confirmed silent sites — interest-system.ts:290 (loot destructuring), npc-movement-system.ts:89.
Task 2: outside the brief's file list, justified — packages/server/src/profile-types.ts converted (PlayerProfile extends WorldPosition; otherwise PlayerRuntime.y = elevation while the inherited saved-profile field still meant ground, and toProfile could not carry z).
Task 2: brief-vs-reality — the factory is `newPlayer` not `createPlayerRuntime`; there is no `LootRuntime` (it is `GroundLoot`, given three axes via Omit<LootSnapshot,"x"|"y">).
Task 2: complete (commits 9be00b42..454e6ff4, review APPROVED; 2 Important were controller-side record gaps, amended below)
Task 2: LEDGER AMENDMENT 1 (supersedes the "269 .y reads" criterion above — that was TRUE BUT INCOMPLETE).
  WorldPosition still has x AND y, so it stays structurally ASSIGNABLE TO Vec2. Every engine helper taking a Vec2 therefore accepts a converted runtime entity silently, measuring across (ground x, ELEVATION y). These are NOT `.y` reads, so grepping for `.y` finds none of them.
  Confirmed uncovered call sites, all compiling today, all now measuring the wrong plane:
    - packages/server/src/world/loot-system.ts:30   pointDistance(player, item) > LOOT_PICKUP_RANGE
    - packages/server/src/world/monster-system.ts:211  pointDistance(monster, player)
    - packages/server/src/world/interest-system.ts:135 and :261  visibility radii
    - packages/server/src/world/movement-system.ts:78  movementSpeedAt(...)
  loot-system.ts appears in NEITHER the 34-file compiler list NOR the 269-.y-reads table — zero of each. Nothing pointed any task at it before this line.
  PHASE A COMPLETENESS CRITERION (use this, not the .y grep): every call that passes a runtime entity into an engine `Vec2` parameter must be accounted for. Converting the Vec2-taking engine helpers in Tasks 3-4 is what makes these loud.
Task 2: LEDGER AMENDMENT 2 — the persistence round trip is proven on the SERIALIZE leg only. profileFromAttachment (world-runtime.ts:790-834) builds its position from clampRestoredPosition, an engine fn still returning Vec2, so a reconnecting hero currently loses z. Compiler-loud today (TS2741 at :796) and deferred to Task 3/4 — but NO TEST asserts the return leg. Whoever clears that error can satisfy the compiler while dropping an axis, with a green suite. Task 3/4 MUST add a three-axis assertion to profileFromAttachment/positionFromAttachment when the engine types convert.
Task 2: minor (deferred): profile-types.ts now type-imports back into world-runtime.ts (which references global WebSocket), slightly weakening that module's stated split; right fix is moving GroundVector/WorldPosition into @lindocara/engine at Task 7. GroundLoot's Omit<LootSnapshot,"x"|"y"> is sound and dies when Task 7 converts LootSnapshot; interest-system.ts:290's destructuring stays silent until then.
Task 3: implemented (commit 9e5cd484) — NOT YET REVIEWED.
Task 3: terrain-access 8/8 green. RED captured TWICE — module-missing, then a deliberately naive heightAt implementation that passes 7/8 and fails ONLY the disc test. The brief's fifth test is PROVEN discriminating, not merely asserted.
Task 3: typecheck 200 -> 336 errors (expected direction: converting the junction makes silent sites loud). Exactly 1 error is in Task 3's own files (worldState.ts:333, createNavigationRuntime(TerrainGeometry) — Task 6's signature, left loud rather than papered over with navigation: null). New loud: worldTick.ts +70, WorldRoom.ts +14, monster-system.ts +11, worldEvents.ts +10, movement-system.ts +4.
Task 3: *** PLAN GAP, CONTROLLER RESOLUTION REQUIRED *** — 6 -> 15 red suites, 27 -> 104 red tests. Nine suites join TILE maps and now correctly get close 4007 ("no usable heightfield (absent)"). NO TASK IN THE PLAN assigns giving the test fixtures a heightfield; those suites cannot go green until someone does. ASSIGNED: fold into Task 7 (it already owns the wire + storage + fixtures boundary) and make it an explicit precondition of the Task 8 gate. Task 8 CANNOT pass until this is done.
Task 3: STILL OPEN, carried to Task 4 — clampRestoredPosition/profileFromAttachment not cleared (needs a spawn source ZoneTerrain lacks; body lives in engine/game.ts). Compiler-loud at world-runtime.ts:796/849 and AdmissionService.ts:295. The three-axis RETURN-LEG assertion (Ledger Amendment 2) is still owed.
Task 3: STILL OPEN — loot-system.ts remains at ZERO errors; pointDistance(player, item) still measures ground-x against elevation-y. Task 4 owns it.
Task 3: POSSIBLE BUG introduced — WorldRoom.ts:1481/1569 call zoneFromMapPayload inside try/finally with NO catch, reached via a floating `void this.teleportCrossMap(...)`. The new no-heightfield throw can become an unhandled rejection on a cross-map transition. Review must judge this.
Task 3: beyond the brief — ZoneDefinition.spawns added (so deleting terrain.spawnPoints does not destroy authored spawns); LegacyZoneDefinition added so pixel catalogue zones keep real geometry; engine/test/zone-connectivity.test.ts annotations changed.
Task 3: complete (commits 454e6ff4..9e5cd484, review APPROVED, 0 Critical/Important)
Task 3: CORRECTION — the self-reported unhandled-rejection bug is a FALSE ALARM. Reviewer read both sites: WorldRoom.ts:1150 and :1167-1183 are `void this....catch(err => this.logError(...))`, and the finally blocks at :1517 and :1639 still restore transitioning/authorized. Real residual is smaller: on a throw the hero gets NO zone.transition_failed event, just a silent unfreeze. Both sites are compile-red anyway (:1502 passes ZoneTerrain to clampRestoredPosition, :1588 reads terrain.width) so Task 4/7 must visit them.
Task 3: reviewer VERIFIED the disc test discriminates by computing the cell arithmetic himself (size 4, toCell=floor(w+2); at (-0.5,-0.2) centre is cell (1,1) level 0 so heightAt allows, disc r=0.3 reaches j=2 at dz=0.2 so maxHeightAround refuses). The brief's own grid could NOT express its fifth test; the fixture re-layout is a correction of the brief, documented.
Task 3: minor (deferred): MAX_STEP hardcoded 0 in terrain-access.ts:622 is a SECOND copy of world.maxStep (hero-step.ts:51 / apps/lab/settings.ts:34) — two places to change, compiler names neither; staticColliders now shares rect objects with the index (array snapshotted, elements not); ZoneDefinition is a mixed-unit type (terrain/spawns tile, monsters/guards/portals/questSites still pixel Vec2 but all set to [] on live zones); `markers` still passes through where layers/elements/events were blanked; test polish (new Array(16).fill types as any[], no HEIGHT_EPSILON boundary test).
Task 3: *** RISK FOR TASK 4 *** — canStand's escape hatches (canEnter has two, hero-step.ts:87-88 and :93) are documented as the CALLER's rule but pinned by no test, and canStand has zero production callers yet. Task 4 restores heroes at persisted positions with a groundY nothing on the server currently owns. A hero restored inside a plateau's disc is PERMANENTLY IMMOBILE with no diagnostic. Cover this.
Task 4: implemented (commits ba4c3d98, ba0e22e0) — NOT YET REVIEWED. Typecheck 336 -> 297.
Task 4: CLOSED clampRestoredPosition/profileFromAttachment/AdmissionService WITH the owed three-axis return-leg assertions (Ledger Amendment 2 discharged). Hand-wrote a migration (monster_speed -> real); check:migrations clean.
Task 4: escape hatch + wall slide + abandonment + NPC cliff all mutation-verified.
Task 4: *** PLAN GAP 2, CONTROLLER RESOLUTION *** — pointDistance/withinRange NOT converted; 49 server sites remain SILENT (Ledger Amendment 1 still open). Implementer declined because converting them would redden three currently-green unowned packages. That is a real trade, not laziness. ASSIGNED: Task 7 owns converting the engine Vec2 helpers, and its brief must carry the grep recipe from task-4-report.md. Task 8 CANNOT pass with these unaccounted.
Task 4: *** ACCEPTED BROKEN STATE, Task 6 MUST CLOSE *** — NO MONSTER CHASES ANYTHING right now. DEFAULT_ZONE_NAVIGATION.waypointTolerance/targetMoveThreshold are still pixels, so every destination reads as already reached.
Task 4: monster-system.test.ts NOT converted (10 -> 21 failures), blocked on Task 6 — six tests are about the pixel A* grid, two about the retired safe zone. Task 8 must not pass with it red.
Task 4: patrolRadius still arrives in PIXELS from worldEvents.ts:288 (Task 5) while consumers now read tiles.
Task 4: the safe zone did not survive the heightfield; worldTick.ts and peasant-support-system.ts still call safeZoneShelters and must make the same call (unassigned — Task 5 or 7).
Task 4: IDE diagnostics flag movement-system.ts:114/:170 (Vec2 boundaries, believed Task 6's) and unused groundOf/planarOf at :56/:60 — review must confirm whether the unused helpers are dead code left behind.
Task 4: review: NEEDS FIXES — 1 CRITICAL, 3 Important.
  CRITICAL: terrain-access.ts:275,278 resolveGroundMovement passes groundY = groundUnder(CANDIDATE), so canStand's centre test surface > groundY + MAX_STEP*levelHeight + eps is SELF-SATISFYING and can never fail. Any step longer than BODY_RADIUS (0.25) climbs a cliff. REPRODUCED against the task's own fixture: a ghost (GHOST_SPEED 0.264 tiles/tick) climbs in 9/200 straight approaches; a knockback >= 0.25 tiles does it deterministically. Masked only because a living hero's 0.203 step is under the disc radius. Same mistake the author correctly AVOIDED in npc-movement-system.ts. Fix = pass ground under `from`.
  Important 2: map-hero-settings.ts:5352 narrowed movementSpeed to {80/64, 520/64} while MONSTER_TUNING_LIMITS.speed was deliberately left at pixel range for the same reason. Stored maps silently revert to defaults on read; an unmodified PUT 400s. The covering test (maps.test.ts "upgrades a persisted four-class hero profile") was converted 341 -> 341/64, removing the coverage.
  Important 3: movement-system.ts:114 heldBlink now bypasses terrain ENTIRELY; the comment claims rematerialisation validates it, but safeLumenLanding (worldTick.ts:2238, unconverted Task 5 code) writes player.y = landing.y as a GROUND axis into the elevation field.
  Important 4: engine/navigation.ts's waypointTolerance/targetMoveThreshold are plain numbers in a sibling of six engine files this task already converted, and the task's own suite writes the conversion out. Deferring left this task's own subject matter dead.
  CONFIRMED NOT DEFECTS: groundOf/planarOf are live (6 call sites; IDE misprojection). Migration verified correct and belonging here (snapshot chain, single ddl delta, FK handling). The pointDistance trade was right. loot-system.ts:30 genuinely closed.
Task 4: fix round 1/5 dispatched (resumed implementer aae3fa3b127e24b3c)
Task 4: fix round 1/5 (all addressed, 0 open; commits ba0e22e0..9f1ff592). Re-reviewer INDEPENDENTLY verified the mutation claims with his own probe: ghost sweep catches the reverted resolver in 1/25 phases, knockback deterministic (z=3.5 fixed vs 4.5 mutated), thin-body groundPathClear discriminates while the BODY_RADIUS one is a non-discriminating anchor (as its own comment says).
Task 4: the implementer found the SAME trap a second time unprompted — groundPathClear re-grounded each sample, so a straight line up a cliff read as walkable. Now carries the ground forward. Two other self-grounding sites (restoreStandablePosition, AdmissionService.ts:140) verified correct by construction: positional "could a body be standing here", nobody is stepping.
Task 4: "no monster chases anything" is WITHDRAWN — engine/navigation.ts geometric fields converted, suite override deleted, chase tests now run against the real DEFAULT_ZONE_NAVIGATION.
Task 4: complete (commits 9e5cd484..9f1ff592, review clean after fix)
Task 4: minor (deferred): the ghost sweep passes with thin margin (window stride-radius = 0.0141 vs sample spacing 0.0106) — if GHOST_SPEED_MULTIPLIER fell from 1.3 to ~1.27 the test would SILENTLY stop discriminating while the premise test still passed; derive the phase count from the window to make it self-maintaining. editor MapHeroSettingsDialog.tsx:211 offers a 1.25-520 slider for a field the server now reads as tiles (editor quarantined, nothing ships). movement-system.ts:116-121 comment blocks run together.
Task 4: STILL OPEN for Task 6 — monster-system.test.ts unconverted (blocked on the pixel A* grid). STILL UNOWNED — MonsterSpawn/GuardDefinition still extend Vec2.
Task 5: implemented (commits 5527b5da, 695071dd, 1c55a70f, 046575ff, 6f4c2622, 6b191226) — NOT YET REVIEWED. *** RESUME POINT: package base 9f1ff592..HEAD and dispatch the task reviewer. ***
Task 5: typecheck 297 -> 285. Task 5's own eight files at ZERO, plus rogue-shadow-dance, combat-action, authored-monster/guard, terrain-access. Server 114 failed / 14 files (baseline 115/15).
Task 5: all four carry-forwards CLOSED — patrolRadius at its definition (authoredPatrolRadius + authored spawn positions); safe zone decided and documented in game.ts + root AGENTS.md; safeLumenLanding closed; Vec2 sites 49 -> 45 (the bigger closure was converting directional-combat.ts's geometry to GroundVector, which made the rest loud rather than shrinking the grep).
Task 5: found and fixed a THIRD instance of the self-grounding trap on its own (commit 6b191226, harvest sight test grounded on nothing).
Task 5: *** PLAN GAP 3, CONTROLLER RESOLUTION *** — worldTick.ts (136 errors, the single largest remaining) is in NO task's file list, and Task 8 cannot pass without it. Task 7 is already carrying wire + storage + bridges + the nine heightfield fixtures + the engine Vec2 helpers; adding worldTick would overload it. RESOLUTION: worldTick.ts needs its OWN task, inserted between Task 6 and Task 7. Whoever resumes should write that brief before dispatching Task 7.
Task 5: BALANCE CHANGE to confirm — MONSTER_BODY_HITBOX's vertical offset moved from ground-north to ELEVATION. Argued in the table's comment, flagged by the implementer as overrulable. A reviewer or the author should confirm this is wanted; it is not a pure unit conversion.
Task 5: GroundIndexQuery/GroundIndexUpdate added to narrow the systems' dependency on Task 6's grid so three suites stay green. IDE flags skill-system.ts:14 importing GroundIndexUpdate — review must confirm it is exported.
Task 5: IDE also flags world-runtime-axes.test.ts:74/:91 (MonsterSpawn/GuardDefinition still extend Vec2 — still unowned) and game.test.ts:822-855 (GroundVector vs Vec2). Review must confirm these are expected in-flight state, not Task 5 breakage.
*** PLAN AMENDED (author-approved) ***
  - MONSTER_BODY_HITBOX vertical offset moving from ground-north to ELEVATION: APPROVED by the author. Not a defect; do not re-raise.
  - worldTick.ts now has its OWN task = Task 7 (new). EVERYTHING AFTER SHIFTED BY ONE.
    RENUMBER MAP (old -> new): 7->8 wire/storage/bridges | 8->9 THE GATE | 9->10 wire movement inverts | 10->11 client owns the hero | 11->12 mobility client-applied | 12->13 remote state + docs.
    Every earlier ledger line saying "Task 7 owns X" now means TASK 8. Every line saying "Task 8 is the gate" now means TASK 9.
  - Plan Gaps 1 and 2 are no longer ledger-only: they are written into Task 8's brief in the plan itself (nine fixtures need a heightfield; convert the engine Vec2 helpers).
Task 5: review: NEEDS FIXES — 0 Critical, 3 Important.
  Imp 1: priest-variant-system.ts:129/:154 — pixel-era Math.max(1, ...) floors now CLAMP converted tile values. Lumen Gate trigger 28px -> 1 tile (2.3x); Sacred Passage trail 22px -> 1 tile wide (2.9x, heal corridor 0.59 -> 1.25 tiles). Authoritative combat geometry, no test anywhere near it. The report claimed this class was swept — the sweep covered peasant-support-system.ts ONLY. Fix + tests + widen the sweep to all eight files.
  Imp 2: worldTick.ts:2231 — lumenLandingClear's NPC clause compares TILE center against PIXEL eventCellCentre (col*64+32); distance always in the hundreds vs a 0.5 threshold, so it can NEVER reject. A dead authority check that reads as live. The excuse (grid size unavailable) is refuted eight lines below: safeLumenLanding already holds zone(...).terrain whose .size is the wanted argument.
  Imp 3: peasant-harvest-system.ts:314 — Math.min(skillRange, target.profile.range) takes min of tile (~0.84) and pixel (tens), so authored node reach is inert; nodes authored short are harvestable from full skill range.
  VERIFIED GOOD: conversion at definitions; sweep still swept and tested against both degradations; NO ground-meaning `y` survives in the eight files; all four carry-forwards genuinely closed (safeLumenLanding now returns a WorldPosition whose y is groundUnder(...)); NO FOURTH self-grounding instance; typecheck 285 reproduces with all eight files at 0 and Task 6/7 files untouched.
  CONFIRMED NOT DEFECTS: MONSTER_BODY_HITBOX elevation offset (author-approved); GroundIndexQuery/GroundIndexUpdate exported and a sound seam (IDE flag was a false positive).
Task 5: fix round 1/5 dispatched (resumed implementer a00c6a41f3c86d0ab)
Task 5: fix round 1/5 (all addressed, 0 open; commits 6b191226..71b7a91f). Re-reviewer independently verified BOTH floor tests discriminate (restoring Math.max(1,...) makes triggerRadius/width assertions fail, and the lumenTrailTouches behavioural probe fails on its own), re-ran the widened floor grep over all eight files himself and confirmed every surviving non-zero floor is a count/duration/HP/damage/power/ratio, and confirmed the authored-reach two-half test has real margin (1.875 selects, 0.844 refuses, 4.6875 skill range swings both without the conversion).
Task 5: complete (commits 9f1ff592..71b7a91f, review clean after fix)
Task 5: CARRY TO TASK 8 — hasPeasantHarvestLineOfSight gained a REQUIRED groundY parameter, and a FOURTH caller exists at world-room-events.test.ts:638 still passing three arguments (the fix report said "three test call sites"). Not new breakage — that file was already red and its count is unchanged at 17 (a TS2554 arity error replaced a pre-existing GroundVector shape error) — but whoever converts that suite inherits it. Add to the fixture-heightfield recipe.
NOTE — commit cf5a016b is the AUTHOR'S OWN, not a subagent's: re-vendored alepha AND followed Lore's campaign -> project rename in apps/main/alepha.config.ts (campaignId: 63 -> projectId: 63). It rode into Task 5's review package (~3250 of 4380 diff lines) and was NOT verified by that review. It is a DEPLOY-PATH change; both halves must move together or a deploy throws rather than going elsewhere. Verify before the next deploy.
*** BRANCH MERGED TO MAIN (author-approved) *** — main fast-forwarded to 71b7a91f, local s3-tile-units deleted, work continues DIRECTLY ON MAIN. main is 15 commits ahead of origin/main and is DELIBERATELY NOT PUSHED: pushing triggers CI deploy, and the tree is mid-conversion (285 typecheck errors, 114 failing server tests, all by design). PUSH ONCE THE TASK 9 GATE IS GREEN, then delete origin/s3-tile-units (until then it is the only offsite copy of these commits).
Task 6: implemented (commit 061a7fcb) — NOT YET REVIEWED. Typecheck 285 -> 253; server 118 failed -> 92 failed, same failing-file set minus three.
Task 6: SpatialEntity conversion generated 49 NEW errors across 6 files (19 in worldTick.ts, incl. :795/:810 where SPATIAL_EVENT_RADIUS now demands a GroundVector) and SILENTLY FIXED 4 grid-update call sites that had been passing ground vectors into Vec2 — the silent-site class was real, and this is the proof.
Task 6: RED captured by reverting neighbors() to its pre-task filter — all 4 tests fail and the budget test reads 196 vs 180, so the pacing guard discriminates.
Task 6: *** EIGHT TESTS DELETED *** — three safe-zone, three pixel-tilemap-extent, one tile-coarsening, one melee-through-relief now geometrically impossible (BODY_RADIUS vs GUARD_ATTACK_RANGE). Each documented where it stood. REVIEW MUST VERIFY each deletion is genuinely obsolete rather than inconvenient — this is the single easiest way for a conversion to hide a regression.
Task 6: concerns to judge — (1) groundLineOfSight's 0.5-tile sampling stride under-samples melee-length segments; (2) water refuses only a body's CENTRE, so a water wall is 0.25 tiles thinner than a relief one (two converted tests needed a plateau instead); (3) nearestWalkableNode snaps without regard to tier, left untouched to keep pacing identical.
Task 6: remaining 253 — worldTick.ts 150 (Task 7), world-room-combat.test 31 / world-room-events.test 24 / WorldRoom.ts 14 / worldEvents.ts 12 (Task 8), interest-system.ts 5 (4 wire -> Task 8, 1 STALE guard.homeY left from Task 4).
Task 6: review: NEEDS FIXES — 0 Critical, 2 Important. Pacing VERIFIED unchanged (every constant checked); 7 of 8 deletions confirmed properly obsolete; NO fourth self-grounding instance (createNavigationGrid's self-grounding is correct — it asks "could a body stand here", not "may a body step there").
  Imp 1: the "geometrically unreachable" justification for deleting the melee-LOS test is FALSE. The corner figure was wrong by >2x — maxHeightAround is a DISC test, so around a plateau corner the standable region pinches to >0.25 from the corner and minimum separation tends to 0.25*sqrt(2)*2 = 0.707, not 1.9. Reviewer built a passing counterexample: guard (-0.2517, 0.3317), monster (0.3317, -0.2517), dist 0.8251 < GUARD_ATTACK_RANGE 0.84375, both standable, groundLineOfSight false. The rule is live at monster-system.ts:318 and :558 with ZERO melee-range coverage now. Restore.
  Imp 2: groundLineOfSight's 0.5-tile stride false-clears by CORNER GRAZING at any length, not just on short segments. Measured against a 0.005 reference around a 2x2 plateau: 1,410,994 false-clear pairs of 32,786,866 within 2.5 tiles (4.3%); 180 of 1,518,789 within melee range. Projectiles are saved by the swept test; MELEE HITS LAND THROUGH THE CORNER. terrain-access.ts is Task 3/4's file, so the fix may be carried — but with THIS evidence, not the milder framing.
Task 6: fix round 1/5 dispatched (resumed implementer a5363f3c75aaad8b9)
Task 6: fix round 1/5 applied (commit 07370f9d) — *** RE-REVIEW NOT YET RUN. RESUME POINT: package 061a7fcb..07370f9d and dispatch the scoped re-review. ***
  Imp 1 FIXED: implementer conceded the corner arithmetic was wrong by >2x (reasoned about maxHeightAround as excluding the whole cell, when it is a closest-point DISC test). Melee LOS test restored on a plateau corner at the reviewer's coordinates, with groundDistance < GUARD_ATTACK_RANGE, both canStand true, and groundLineOfSight false asserted INLINE so the witness cannot rot. Docblock corrected to seven deletions.
  Imp 2 CARRIED, NOT FIXED (correct call — terrain-access.ts is Task 3/4's, and the real answer is a DDA cell traversal, not a smaller stride). Re-measured rather than quoting: a 0.1 grid finds ZERO melee false-clears (it misses the band entirely); at 0.01 around one corner, 2,314 of 20,225,422 melee pairs false-clear, longest at d=1.676. Witness pair (-0.33, 0.28) <-> (0.31, -0.26), d=0.8374.
  Also fixed: budget test split into cap + parity (suite now 5); NavigationGrid.cellSize dropped, nodeForPoint is Math.floor(point.x + half); report narrative corrected.
  Tests: monster-system 26/26, navigation 17/17, server 92 failed | 439 passed; typecheck 253, none in Task 6's files.
NOTE — uncommitted .vendor/* + package-lock.json changes are the AUTHOR'S OWN vendor sync in progress, NOT an agent's. The Task 6 implementer correctly refused to sweep them into its commit and split them back out. Leave them alone.
Task 6: fix round 1/5 (all addressed, 0 open; commits 061a7fcb..07370f9d).
Task 6: complete (commits 71b7a91f..07370f9d, review clean after fix)
Task 6: re-reviewer INDEPENDENTLY reproduced the stride measurement — standable 7,405 / melee pairs 20,225,422 / false-clear 2,314 at step 0.01, and ZERO at step 0.1 — exact match, so the counterintuitive result is a correct observation, not a broken harness. He also found a STRONGER argument for carry-don't-fix than the implementer had: sampleSegment clamps at Math.min(512, ...) (terrain-access.ts:198), so shrinking the stride silently reverts to a COARSE effective stride on long segments. A DDA cell traversal is the only shape that closes it. Whoever picks that up must know the 512 cap exists.
Task 6: restored melee LOS test verified discriminating — monster-system.test.ts:421-448, four inline assertions at :437-440, d=0.825052 < GUARD_ATTACK_RANGE 0.84375, both canStand true, groundLineOfSight false; sampleSegment takes steps=2 and the midpoint (0.04,0.04) sits squarely inside the plateau cell. The end-to-end half is load-bearing: advanceGuards gates on distance AND sight, and distance is asserted in range, so a missing startAttack can only be the sight rule.
Task 6: minor (deferred): monster-system.test.ts:424-425 says the closest point "is that corner" when for these two bodies it is an EDGE point ((0.3317,0) and (0,0.3317)); distances and conclusion unaffected, but worth tightening in a fix whose whole point is closest-point reasoning.
Task 6: typecheck is 252 (report said 253 — one-line discrepancy in a hand-off scope note, immaterial). interest-system.ts:270 still reads guard.homeY where GuardRuntime has homeZ — a TASK 4 LEFTOVER, still unowned.
NEXT: Task 7 (worldTick.ts, ~150 errors). Then Task 8, then the Task 9 gate.
Task 7: implemented (commit 30386311) — NOT YET REVIEWED. *** RESUME POINT: package 07370f9d..30386311 and dispatch the task reviewer. ***
Task 7: worldTick.ts 150 -> 0, worldEvents.ts 12 -> 0. Repo-wide server typecheck 252 -> 91 (verified independently). Lint clean (the IDE's unused-import flags on worldTick.ts are the documented tsserver misprojection). New world-tick-tile-units.test.ts 11/11, each rule pinned by a mutation proof. Server suite unchanged from baseline at 12 failed files / 92 failed tests, +11 passing. Engine 885/885.
Task 7: SILENT Vec2 — 43 closed (all of worldTick.ts's). 3 remain repo-wide, ALL in interest-system.ts (:135, :148, :261), plus 5 in the already-red world-room-combat.test.ts.
Task 7: tick order verified UNCHANGED — advanceWorldTick extracted from both revisions and diffed byte-identical; declaration order identical bar the deleted pixel-only bodyCenter.
Task 7: *** PLAN GAP 4, CONTROLLER RESOLUTION *** — WorldRoom.ts carries 15 errors and belongs to NO task; the gate cannot route around it. RESOLUTION: assign to Task 8 (it already owns the wire/storage/bridges/fixtures/Vec2-helpers, and WorldRoom.ts's errors are wire-shaped — planarOf gained 8 wire-boundary call sites and groundOf 5 legacy-catalogue ones, all of which die with Task 8's protocol work). Whoever resumes must add this to Task 8's brief.
Task 7: additive engine change — harvestGroundColliderAt added to packages/engine/src/harvest.ts because worldEvents' collision had no ground-typed producer. Additive only; review should confirm.
Task 7: PROCESS NOTE — the implementer ran one `git stash`/`pop` to measure a baseline while the owner was committing in parallel. It round-tripped cleanly but was riskier than needed. Future briefs should forbid stash on this repo; measure a baseline with `git stash create` + `git diff` or a second checkout instead.
Task 7: committed via `git commit -- <paths>` so the owner's staged apps/lab/src/boot.ts and their .vendor/, package.json, package-lock.json, AGENTS.md and apps/lab/* changes stayed untouched. Correct discipline.
NEXT: review Task 7, then Task 8 (now also owning WorldRoom.ts's 15 errors), then the Task 9 gate.
Task 7: review: NEEDS FIXES — 2 CRITICAL, 2 Important.
  CRIT 1: worldTick.ts:4455-4459 applyLumenPortal writes player.x and player.y (elevation) but NEVER player.z. safeLumenLanding returns a WorldPosition whose ground pair is x/z, so the hero moves on one ground axis, keeps its pre-teleport z, and takes the landing's elevation — at a position canStand/lumenLandingClear never validated (they checked destination.x/destination.z). Typechecks because both sides are number.
  CRIT 2: worldTick.ts:2637-2638 rogue shadow return, same shape — player.x and player.y written, player.z never. planShadowReturn returns {x, y: groundUnder(...), z}, and :2664 two lines below DOES read destination.z for the animation heading, so the run reports a direction the body never took.
  Reviewer enumerated every .x=/.y=/.z= assignment in both files: these are THE ONLY TWO where .x= is not paired with .z=. The other eleven position writes are correct.
  Imp 3: harvestGroundColliderAt (engine/src/harvest.ts:432) has NO test anywhere — new exported pure fn doing this increment's exact arithmetic in a package with an 885-test suite.
  Imp 4: bridge census undercounted — planarOf gained 15 sites (not 8), groundOf 6 (not 5). All judged honest bridges that die with Task 8, but ground.ts:66 makes the census a governance signal and Task 8 sizes against it.
  VERIFIED GOOD: tick order byte-identical from advanceWorldTick to EOF (extracted from both revisions and diffed); silent Vec2 census genuinely zero in both files, 3 left repo-wide (interest-system.ts:135,148,261); NO fourth self-grounding step/sweep (every canStand call enumerated across server+engine); mutation proofs discriminate; harvestGroundColliderAt arithmetic hand-verified correct; no Task 8 file touched to lower the count.
Task 7: fix round 1/5 dispatched (resumed implementer a6a40168fc7b506ef)
Task 7: fix round 1/5 (all addressed, 0 open; commits 30386311..041cbf6a — note the owner landed 5 unrelated commits inside that range; the implementer staged only its own paths).
Task 7: complete (commits 07370f9d..041cbf6a, review clean after fix)
Task 7: re-reviewer INDEPENDENTLY re-ran the .x=/.z= pairing scan — 14 .x= sites across both files, every one paired, AND checked the write forms a dot-property grep misses (Object.assign, obj["x"], .position =): none exist. Both Critical fixes pinned by tests whose destinations differ on BOTH ground axes (gate 1.5,1.5 -> -2.5,-2.5; shadow 1.5,1.5 -> -3.5,-2.5), driven through the real advanceWorldTick rather than the private fn. Ricochet revert verified byte-faithful (base was monster.x + PLAYER_SIZE/2 on a top-left-anchored pixel body = the body centre; now monster.x/z in tile units = the same point). Census independently counted: planarOf 15, groundOf 6, both 0 before the task.
Task 7: cost note (not a finding) — the broadened handleRelease guard makes nearestStandableCell (an O(size^2) full-grid scan) run on every release where the anchor is unstandable, not only when the player died on the spawn. Release is rare and player-initiated, so this is a cost note only.
STATE AFTER TASK 7: server typecheck 91 errors — world-room-combat 31, world-room-events 24, WorldRoom.ts 15, interest-system 5, rest 1-4. Server suite 92 failed / 453 passed.
NEXT: TASK 8. Its brief must carry, beyond the plan text: (a) WorldRoom.ts's 15 errors (assigned here, plan gap 4); (b) the nine heightfield test fixtures; (c) the engine Vec2 helpers; (d) the 3 SILENT pointDistance sites in interest-system.ts:135/:148/:261 — they measure ground x against elevation y, produce no compiler error, and NOTHING will flag them when the wire is retyped; (e) the 15 planarOf / 6 groundOf bridges in worldTick.ts, which die with the protocol work; (f) hasPeasantHarvestLineOfSight's fourth caller at world-room-events.test.ts:638. Then the Task 9 GATE.
Task 8: implemented (commits 3116b2e4, 5e06bc71, c160e7f5, e2e028c5, 4b876e13) — NOT YET REVIEWED. *** RESUME POINT: package 041cbf6a..4b876e13 and dispatch the task reviewer, THEN Task 9 (the gate). ***
*** PHASE A IS COMPLETE AND GREEN — CONTROLLER-VERIFIED INDEPENDENTLY, not taken from the report:
      npm run typecheck  -> 0 errors across ALL programs (peak was 336; 91 before this task)
      npm test           -> 221 files / 1944 tests, ZERO failures (92 were failing before this task)
      npm run lint       -> clean
      WorldInfo.tiles and WorldInfo.colliders: GONE from protocol.ts
      Both bridge FILES deleted (tile-pixel-bridge.ts, heightfield-pixel-bridge.ts) — confirmed absent.
      One "TILE→PIXEL BRIDGE" string remains, at protocol.ts:520, and it is PROSE in a docblock explaining that the bridge is gone. Not a live conversion site. Review should decide whether to reword it so the grep sweep stays meaningful. ***
Task 8: the THREE SILENT interest-system.ts sites are CLOSED. The implementer deleted pointDistance/withinRange outright rather than converting their signatures, which made them loud. All three were measuring |dx| ALONE — the ground z was never consulted. That class is now closed.
Task 8: found and fixed SEVEN more silent half-conversions no compiler could see — three parser branches keyed on "y" (would have dropped every camp/orb/special-impact frame), the {t:"event"} frame's axis (22 sites shipping elevation as ground), the camera focus, and both halves of world-delta.ts. Two constants had also outlived their units: WORLD_POSITION_DELTA_THRESHOLD and DIALOGUE_CLOSE_RADIUS (192 tiles — the walk-away close could NEVER fire).
Task 8: STRUCTURAL DECISION beyond the brief — terrain-access.ts MOVED INTO THE ENGINE so client prediction and the server share one collision rule. Review must judge this; it changes the package graph.
Task 8: *** CONCERN FOR THE GATE *** — authored events NEVER reach a heightfield room (zoneFromMapPayload bakes events: []), so exits, teleporters and harvest nodes are DEAD in production until a later task. Two suites carry a documented seam around it. NOTHING ELSE WOULD TELL YOU. Task 9 must confirm this is understood and accepted, or it becomes a silent regression.
Task 8: cross-map transitions now land at the destination's OWN SPAWN (a tile-editor cell cannot address a heightfield grid). Worth exercising at the gate.
Task 8: review: NEEDS FIXES — 1 CRITICAL, 2 Important. Reviewer's gate verdict: "No — one line away from yes."
  CRITICAL: packages/engine/src/cooperation.ts:9 REWARD_DISTANCE = 900 is compared against groundDistance (TILE units) at worldTick.ts:1340/:1348/:1359. 900 tiles is ~14x the widest grid, so ALL THREE reward-eligibility gates are permanently true — every alive contributor, the killer, and every alive party member ANYWHERE on the map gets XP/loot/quest credit for every kill. The 14-tile proximity rule docs/cooperative-combat.md describes no longer exists. THREAT_LEASH_DISTANCE four lines above WAS converted, so this is a scope-of-search miss, not a judgement call. No test references REWARD_DISTANCE at all.
  Imp 2: TestSessionService.ts:83-84/:247-248 still writes PIXEL coords into the converted hero row — tileCenter returns {x: col*64+32, y: row*64+32} written to heroes.x/heroes.y, where y is now ELEVATION and z is never written. The one write path that undoes the migration. Invisible because restoreStandablePosition rejects canStand(terrain, 2176, 0, ...) and falls back.
  Imp 3: the dead authored events (worldState.ts:165 bakes events: []) are marked only in a task report and a test docblock. This diff EDITED root AGENTS.md's known-broken blockquote and did not extend it. Needs a durable banner before the gate.
  VERIFIED GOOD (reviewer checked at the base commit, not from the report): the three silent sites were REAL — pointDistance read .x/.y, both operands were WorldPosition whose y was already elevation, so it computed |dx| alone. Deleting the export rather than converting the signature was the ONLY lever that would have worked given structural assignability. DIALOGUE_CLOSE_RADIUS arithmetic confirmed (192 tiles). WORLD_POSITION_DELTA_THRESHOLD's 50-quanta ratio preserved exactly. Migration verified against the snapshot chain (prevIds chains; ddl delta is exactly two columns). terrain-access.ts's move verified PURE mechanically, and its shared-rule claim verified by reading both movement paths side by side — INCLUDING that both dropped movementSpeedAt TOGETHER, which a half-done version would have made the increment's fourth silent desync. The reviewer ran his own pairing scan: 4 unpaired .x= sites, all four water-UV scroll offsets in terrain-visuals.ts, correct as they stand.
  ORDERED WITH THE FIX: a full sweep of every numeric literal compared against groundDistance/withinGroundRange/a squared distance, each needing a / TILE_SIZE or a comment saying why not. Report said "two constants"; it was three.
Task 8: fix round 1/5 dispatched (resumed implementer aa32ce68b89b4bd8a). THE GATE (Task 9) MUST NOT RUN UNTIL THIS CLOSES.
Task 8: fix round 1/5 (all addressed, 0 open; commits 4b876e13..a6ae95f3). Re-reviewer verdict: SAFE TO GATE = YES.
  Critical closed structurally, not just numerically — the rule is now withinRewardDistance() in cooperation.ts delegating to withinGroundRange, all three tick call sites use it, and REWARD_DISTANCE's import is gone from worldTick.ts. Mutation-proved twice.
  Re-reviewer RE-SWEPT INDEPENDENTLY (every groundDistance/withinGroundRange/groundDistanceSq call site outside tests) and confirms NO SECOND SURVIVOR. Only raw literals left in a ground comparison are 2 * BODY_RADIUS, derived from a converted constant. Authored HarvestProfile.range stays pixels by design and crosses at authoredReach.
Task 8: complete (commits 041cbf6a..a6ae95f3, review clean after fix)
Task 8: FOLLOW-UPS (recorded, non-blocking): (1) ground-distance-units.test.ts's "catch-all" is a HAND-WRITTEN array of 17 names, not an enumeration — its docblock at :196-200 claims completeness it does not have (BODY_RADIUS, MAX_MONSTER_BODY_REACH, MAX_PROJECTILE_RANGE absent; 8 server-local constants structurally unreachable from an engine test, so its home should be the server package or a mirror). Fix the docblock or move the guard. (2) ground-distance-units.test.ts:85 is a tautological assertion that always passes — meant to surface the offending name, does not. (3) TestSessionService.ts:279's three-axis guard is UNPROVEN: the fixture's requestedMap differs from the hero's created map, so mapId alone fires the update and deleting the z comparison leaves the suite green. (4) WorldRoom.ts:1869 still ships cemeteries: [...CEMETERIES] — three PIXEL coordinates — on every welcome, validated by protocol.ts:1604. Inert (no reader found) but it is the one place a future reader could take pixel coords off the wire and believe them.
*** GATE (Task 9) NOW RUNNING ***
*** TASK 9 — THE GATE: **PASS** (marker commit ae64043e) ***
  npm run v exit 0 — CONTROLLER RE-RAN IT INDEPENDENTLY, "verify executed successfully". 221 test files / 1947 tests, 0 failures. Migrations "No changes detected". Catalog + map checks + build green.
  CLIFF BLOCKS (the phase's headline): held D into the plateau's west face 4s -> (-2.000, 0, -5.141) to (-1.391, 0, -5.141); 6 MORE seconds -> IDENTICAL TO 4 DECIMALS, y stayed 0. Three more faces the same. Each stop lands exactly on the decoded level-1 cell boundary from the STORED heightfield. CONTROL: walking west immediately after moved 8.7 tiles — so it is collision, not a dead input socket.
  WATER BLOCKS: (1.656,0,-11.641) -> (1.656,0,-13.875), then 4 further seconds move ZERO.
  Screenshots: scratchpad/shots/01-terrain, 02-walked, 03-cliff-blocked, 04-cliff-blocked-north, 05-water-blocked, 06-plateau-corner.
  Expected temporary state confirmed: high ground unreachable (MAX_STEP 0, jump is Phase B). Accepted costs re-confirmed: events: [] bake, the AGENTS.md banner (lines 92-111, accurate), pixel cemeteries with no reader.
  COULD NOT CHECK: monsters — a heightfield room bakes monsters: [], live room reports 0. Stated, not implied.
  NOTE: the OWNER'S dev DB is stale (hero creation 500s with "table heroes has no column named z"). The gate did NOT touch it — played on a fresh scratchpad DB. One earlier additive run did hit the owner's DB (idempotent re-stamp + one guest account/party, no hero).
*** PHASE A COMPLETE. Tasks 1-9 done. NEXT: Task 10 (the wire's movement half inverts), 11 (client owns the hero), 12 (mobility client-applied), 13 (remote state + docs). PUSH main NOW — the gate is green and this was the agreed push point; then origin/s3-tile-units is already deleted. ***
*** PHASE A PUSHED — main 13f058e2..ae64043e is on origin. The gate was the agreed push point; origin/s3-tile-units was already deleted. CI deploys from a GREEN tree. ***
Task 10: implemented (commit 79740712), review: APPROVED with 2 Important. First Phase B task — protocol shape only.
  Task 10 DEVIATED FROM ITS BRIEF AND WAS RIGHT: the brief said facing: Vec2, and Vec2 is {x, y} — the increment's central trap sitting in the instruction. It used GroundVector {x, z}. Reviewer confirmed consistent across type, validator and parse-return.
  Imp 1: THREE clauses this task added have ZERO coverage — reviewer mutated an isolated copy against the full 862-test engine suite and all three pass with the clause disabled: protocol.ts:1780 hasOnlyKeys, :1782 isDirection(value.facing), :1170-1172 the three isPlayerSnapshot boolean checks. The facing gap is sharpest: relaxing isDirection to isPosition would silently accept a zero-length or {x,y} heading and nothing would fail.
  Imp 2: MOVE_COORDINATE_LIMIT's docblock (protocol.ts:93) claims "no map may exceed MAP_MAX_COLS a side" — that constant is enforced ONLY by mapAuthoring.ts:93,128, the LEGACY TILE-MAP validator. decodeMap validates size as merely a positive integer (hd2d/map-data.ts:155), so a heightfield of side 400 decodes and a hero past +/-128 tiles would have EVERY FRAME SILENTLY DROPPED. Nothing breaks today (only heightfield in repo is size 72).
  VERIFIED GOOD: error table reproduces exactly (13 server + 15 client = 28 / 8 files, no Task 11 file pre-fixed); absent-key rejection mutation-confirmed on ALL THREE flags; prediction.ts Command move judged forced not creep; the {x,y}-no-z rejection row added to protocol.test.ts:651 pins the half-converted-sender case; world-delta needs no change (visiblyChanged falls through to a whole-object compare, so the three flags diff correctly).
Task 10: *** NEW HAZARD FOR TASK 11, neither of us had listed *** — RATE_MAX_MESSAGES = 35 per RATE_WINDOW_MS = 1000 (world-runtime.ts:86-87). The retired model sent one input per 20Hz tick, comfortably inside. Nothing in MoveMessage states a cadence, so A CONTROLLER REPORTING PER ANIMATION FRAME (60Hz) WILL TRIP THE WINDOW AND BE DISCONNECTED. Fix is a client send cadence.
Task 10: TASK 11 DESIGN INPUTS (from the implementer, reviewer-endorsed): (a) the room still owes a real bounds/canStand check before it stores or relays a reported position — the spec conceded AUTHORITY over movement, not VALIDITY, and no such check exists yet; (b) isDirection demands a UNIT vector, so Task 11 must normalise facing and seed one at spawn or every frame drops silently; (c) the 60Hz rate-limit hazard above.
Task 10: editor un-quarantine checklist — packages/editor/src/game/map-preview.ts:339 still sets ack: 0, compiler-invisible while quarantined.
Task 10: fix round 1/5 dispatched (resumed implementer a4271f5d33eeadd77)
Task 10: fix round 1/5 applied (commit f753605b) — *** SCOPED RE-REVIEW NOT RUN. RESUME POINT: package 79740712..f753605b and dispatch the scoped re-reviewer, THEN Task 11. ***
  CONTROLLER VERIFIED MECHANICALLY (not from the report): typecheck:engine 0 errors, engine 79 files / 894 tests green (was 889), lint clean. IDE flags on protocol.ts:78/:104 are the documented tsserver misprojection.
  Imp 1 closed: four cases added — extra key on move (seq, hp); facing {x:0,z:0} / {x:0,y:1} / {x:3,z:4}; a PlayerSnapshot with each flag omitted then mistyped. Each proved by disabling its clause against the FULL suite (hasOnlyKeys -> 1 fail, isDirection->isPosition -> 1 fail, isPlayerSnapshot booleans -> 1 fail). The {x:3,z:4} case is what makes the isPosition mutation fail on UNIT-NESS rather than only on axis names — that is the sharp one.
  Imp 2 closed, both halves: new MAX_HEIGHTFIELD_SIZE = 256 in hd2d/map-data.ts, decodeMap now REFUSES a larger side (mutation-proved), MOVE_COORDINATE_LIMIT derived from it, docblock rewritten to name the MAP_MAX_COLS mistake. A test pins the two constants together. map-limits.ts deliberately NOT imported into map-data.ts — that file must stay at the bottom of hd2d/'s import graph.
  Blast radius checked: only heightfield in the repo is side 72; typecheck:hd2d, typecheck:lab, test:hd2d (81), test:lab (40) all green.
  Task 11's checklist unchanged and re-counted after the fix: 13 server + 15 client = 28 errors / 8 files.
Task 10: fix round 1/5 (all addressed, 0 open; commits 79740712..f753605b).
  Re-reviewer INDEPENDENTLY reproduced all four mutations against the full engine suite: hasOnlyKeys -> exactly 1 fail (893/894), isDirection->isPosition -> 1 fail, isPlayerSnapshot booleans -> 1 fail, decodeMap bound removed -> 1 fail. MOVE_COORDINATE_LIMIT = MAX_HEIGHTFIELD_SIZE / 2 is a genuine import + arithmetic derivation, pinned by a test.
  CORRECTION to the implementer's own reasoning (documentation nit, not a gap): it claimed facing {x:3,z:4} is what makes the isPosition mutation fail on UNIT-NESS. Not so — the test aborts on its first failing expect, and {x:0,z:0} (evaluated first) already fails under that mutation. The {x:3,z:4} assertion is never reached in that run. Coverage claim still true, reason wrong.
  The "keep map-data.ts at the bottom of hd2d/'s import graph" call was VERIFIED SOUND: every file in packages/engine/src/hd2d/ imports only from within hd2d/, zero exceptions, and map-limits.ts governs the LEGACY TILE-MAP format — a different geometric model. Importing it would be exactly the cross-format coupling the fence prevents.
Task 10: complete (commits ae64043e..f753605b, review clean after fix)
Task 10: RESIDUAL (accepted, worth a follow-up) — MAX_HEIGHTFIELD_SIZE (hd2d/map-data.ts:42) and MAP_MAX_COLS (map-limits.ts:3) are two INDEPENDENT 256 literals matched only by a comment. No test pins them together (unlike MOVE_COORDINATE_LIMIT/MAX_HEIGHTFIELD_SIZE, which is tested). Raise MAP_MAX_COLS for the legacy format and MAX_HEIGHTFIELD_SIZE will not follow, and nothing fails — the same silent-drift mode this round closed, one level removed. The deliberate non-import is what makes it unavoidable without breaking the fence; a cross-package test would close it.
*** TASK 11 NOW RUNNING — the client owns the hero. ***
Task 11: implemented (commit 22fa4fbd) — NOT YET REVIEWED. *** RESUME POINT: package f753605b..22fa4fbd and dispatch the task reviewer, THEN Task 12. ***
*** THE HERO CAN JUMP ONTO A PLATEAU. *** Proven in hero-controller.test.ts: blocked at x<1, y=0 walking; after one jump press x>1, y=0.9. Mutation-checked (remove the jump press -> the test fails). Space is the default binding. This is the case that made "units only" unacceptable at scoping time.
Task 11: CONTROLLER-VERIFIED INDEPENDENTLY — npm run typecheck 0 errors across ALL programs; npm test 222 files / 1939 tests, zero failures. All 28 of Task 10's checklist errors closed plus ~20 cascades. IDE flags on worldTick.ts/net.ts are the documented tsserver misprojection.
Task 11: send cadence = 20 Hz (MOVE_REPORT_MS = TICK_MS), the exact retired command rate, plus identical-frame suppression — well under RATE_MAX_MESSAGES 35/s. The 60Hz disconnect hazard is closed.
Task 11: the ROOM VALIDITY CHECK landed HERE, not Task 12 — withinRoomBounds in applyReportedMove. So the room now bounds a reported position against the real map rather than only the global wire bound.
Task 11: session.ts needed NO edit despite being on the brief's file list — input.current() already carries jump and update() kept its signature.
Task 11: CONCERNS FOR REVIEW AND FOR TASKS 12/13 —
  (1) MOBILITY SKILLS PRODUCE NO DISPLACEMENT until Task 12: the held-blink clamp lived inside the deleted movement branch. Grants/costs/cooldowns still work; Shadow Dance still works (explicit server message). This is a live gameplay regression between now and Task 12.
  (2) HERO EVENTS ARE RETURNED BUT NOTHING PLAYS THEM — footsteps, splashes, canopy. Audio wiring was in neither the brief's file list nor its interface. Needs an owner (Task 13?).
  (3) A hero can now SWIM and no server system knows what swimming means (monsters simply cannot reach a swimmer).
  (4) Gamepad has no jump binding — every face button was taken.
NEXT AFTER REVIEW: Task 12 (mobility skills client-applied), Task 13 (remote heroes draw airborne/swimming/gliding + docs). Then the whole-branch review, then push.
Task 11: review: NEEDS FIXES — 0 Critical, 4 Important. Fix round 1 applied (commit a700ad04). *** SCOPED RE-REVIEW NOT RUN. RESUME POINT: package 22fa4fbd..a700ad04, dispatch the scoped re-reviewer, THEN Task 12. ***
  CONTROLLER VERIFIED: typecheck 0 errors all programs; 222 files / 1941 tests green. main is 4 commits ahead of origin.
  Imp 1+2 FIXED AT THE SHARED ROOT: the echo now compares GROUND AXES ONLY (elevation is deliberately re-grounded by place(), and no server displacement moves y alone), and adopt clears only the dedup latch, never #reportedAt — so the 20/s ceiling is real. Both mutation-proved.
  *** HONEST NEGATIVE FROM THE IMPLEMENTER, worth keeping *** — its first storm test at 10 Hz did NOT detect the #reportedAt reset: at a 100 ms snapshot period the reset unblocks a frame that was already due. The ~30/s only materialises when reports are otherwise SUPPRESSED, i.e. the corpse case. So Important 2 was reachable ONLY through Important 1. The reset is still removed and the retightened test drives an adopt EVERY FRAME to pin the invariant rather than the rate (56 vs 20 under mutation). This corrects the reviewer's model, not just the code.
  Also fixed: the editor's deleted-prediction.js import inlined under a banner, and the FULL second-wave debt (4 items) written into packages/editor/AGENTS.md's un-quarantine checklist; MovementSystemContext.zone/playerGrid removed with their callers.
  Imp 4 (mobility produces no displacement) is plan-scheduled for Task 12 and correctly declared — LIVE REGRESSION on main until Task 12 lands.
Task 11: *** OPEN DESIGN QUESTION FOR THE OWNER — blocks nothing, but shapes Task 12's brief *** — DROWNING IS NOW A FREE CLIENT-SIDE TELEPORT. hero-controller.ts:186-190 returns the hero to options.spawn on a `noyade` event, copied from the lab where it is a DEBUG RESET. Here `spawn` is where the welcome admitted the hero, and the result passes applyReportedMove legitimately because it is in bounds. So a player swims into deep water, holds ~11 s (swim.breath), and teleports to their entry point — past cliffs, past monsters, any distance, no HP cost, no server event. It is WITHIN the movement authority the spec conceded, so nothing is broken; it is a player-reachable mechanic nobody designed. Options: keep it; kill it (drown in place and die properly — the corpse-run system already handles "you died somewhere inconvenient"); or make it cost something. The implementer recorded that the smallest honest fix is making drowning a SERVER outcome rather than a client teleport.
Task 11: CARRIED (not fixed): gamepad analog facing precision lost — net.ts:459-460 reads only the digital booleans, so a stick gives 8-direction facing instead of continuous (the retired movementDirectionFromInput path read axisX/axisY); movement-elevation.test.ts:311 asserts 3 of the 6 retained duties while its docblock claims more; gamepad still has no jump binding (every face button taken).
Task 12: implemented (commit 52aa5fd9) — NOT YET REVIEWED. *** RESUME POINT: package a700ad04..52aa5fd9 and dispatch the task reviewer, THEN Task 13. ***
  CONTROLLER VERIFIED: typecheck 0 errors all programs; 223 files / 1955 tests green; npm run v green end to end. main is 5 commits ahead of origin.
  BLINK RESTORED (the live regression is closed): the server grants via SelfState.mobility (distance + deadline, derived from the LIVE HELD ACTION, so its ABSENCE IS THE WITHDRAWAL — no new message). The controller's setMobility/phase spends it once per action id and lands on a standable cell through the same nearestStandableCell the server's safeLumenLanding uses. Cost, cooldown, resource, invulnerability, trail/portal/heal effects and the channel bound ALL stayed server-side; ClientMessage gained nothing that mentions mobility, so a client cannot fabricate a grant.
  DROWNING KILLED PER OWNER'S RULING: the client teleports nobody. It reports {t:"drowned"} (bare tag, ONE new client message) and the room decides — refused unless that client's own position stream has the hero ALIVE AND SWIMMING, then death in place via killPlayer. Body where it went under, corpse run back. The death state machine does the work it already existed to do. No third authority exception.
Task 12: *** CONCERN THAT WANTS ITS OWN TASK *** — a server-authored displacement can still be UNDONE by an in-flight client `move`. Affects dash, charge, shadow step, lumen portal, ghost release and authored teleport. Code-path analysis, NOT test-proven. The honest fix is a one-place staleness handshake in applyReportedMove (e.g. a server displacement sequence the client echoes, so a move that predates it is dropped). The blink is NOT blocked by this — the client resolves its own landing. Raise with the owner before Task 13 or the whole-branch review.
Task 12: minor carried — ~1 RTT of extra phasing after release; drowning kills with no breath meter in the HUD yet.
NEXT: review Task 12, then Task 13 (remote heroes draw airborne/swimming/gliding + the doc sweep), then the WHOLE-BRANCH review, then push.
STILL OWED: the scoped re-review of Task 11's fix (22fa4fbd..a700ad04) was skipped for context; its fix was verified MECHANICALLY by the controller (typecheck 0, 1941 tests) but not judged. Fold it into the whole-branch review's scope.
Task 12: review: NEEDS FIXES — 0 Critical, 1 Important. Reviewed on opus against the a700ad04..52aa5fd9 package.
  Imp 1: packages/client/test/hero-mobility.test.ts:112 — "does not re-arm a grant it has already spent" DOES NOT DISCRIMINATE. SPEED 6, FRAME 1/60, 30 frames, bound 3 + 6*0.5 + 1e-6 = 6.000001; a re-armed controller travels exactly 3.0 twice = 6.0, which PASSES toBeLessThan(6.000001). Deleting hero-controller.ts:291's `if (grant.actionId === armedActionId) return;` leaves the suite GREEN — the once-per-actionId rule (whose own docblock says that without it a 2.5s channel becomes an unlimited one) is unguarded. Fix: second window carries no input, or a bound the walking hero demonstrably stays under.
  CONTROLLER ADJUDICATION 1 (brief deviation, signed off): the brief's "Removed" line called for dropping the mobilityDistance debit; the diff RE-IMPLEMENTS it as debitHeldMobility (worldTick.ts:1036-1047). Reviewer verified the justification independently — worldTick.ts:6469's `(action.mobilityDistance ?? 0) <= 0` is what ends the channel when the budget is spent, so with no debit it could never fire. Deviation ACCEPTED; the brief was wrong on this line.
  CONTROLLER RESOLUTION of both ⚠️ items: (a) movement-system.ts/rogue-skill-system.ts listed "Modify" and untouched — CONFIRMED NOT A GAP, ledger line 202 records that Task 11 deleted the movement branch the held-blink clamp lived inside; nothing was skipped. (b) drowning is not in Task 12's brief text — CONFIRMED IN SCOPE, it is the author's ruling recorded at ledger line 213/218 and no other task owns it.
  VERIFIED GOOD (reviewer checked, not taken from the report): ClientMessage gained exactly ONE member `{t:"drowned"}` with no fields, so no client message mentions mobility; CLASS_SKILLS.blink.distance = 247.5/TILE_SIZE = 3.87 is well under MAX_MOBILITY_DISTANCE 64, so no pixel value reaches the cap; action.id is crypto.randomUUID() so isWireId() cannot reject a legitimate grant and silently kill every state frame mid-blink; phase() writes the x/z PAIR then re-reads elevation from terrain (the axis-pairing shape prior reviews caught — clean here); the drowning guard is a real cross-check (player.swimming comes from the client's last accepted move, and net.ts queues {t:"drowned"} BEFORE #report()); applyDrowning sets no dirty flag but killPlayer -> freeze does, so a drowning death is persisted like any other; #applyMobilityGrant on welcome is sequenced after #hero is built.
Task 12: minor (deferred): worldTick.ts:1027 debitHeldMobility hardcodes skillId !== "blink" while the grant issuer filters on channelMaxEndsAt/mobilityDistance — a second teleport-shape skill would be granted and never debited, so its channel-end could never fire; derive both from one predicate. worldTick.ts:1036 debits every reported move during the channel including the ~1 RTT of ordinary walking before the grant lands (~0.84 of 3.87 tiles at 200ms), recorded nowhere. net.ts:830 `self.serverNow ?? Date.now()` substitutes the CLIENT clock for a SERVER deadline (unreachable today; should refuse the grant instead). skill-system.ts:22 heldMovementDirection now has no src caller — the same argument this diff used to delete the neighbouring allowWater arm. net-wire.test.ts:444 `selfX > 1` does not discriminate the removed drowning teleport (hero-controller.test.ts:155 does pin it properly); its block comment at :826-827 mis-describes the run. net.ts:430 a corpse floating in water re-sends {t:"drowned"} every ~11s forever (refused by the life guard — dead traffic only). worldTick.ts:2447-2456 safeLumenLanding still writes player.x/y/z on release while the client resolves its own land(), and the next reported move overwrites the nudge, so lumenLandingClear is effectively defeated for the hero's own position — CARRY TO THE WHOLE-BRANCH REVIEW.
Task 12: fix round 1/5 (1 addressed, 0 open; commit 96c3699f). The implementer ABANDONED the reviewer's own suggested fix and was right: stepHero's friction ramp brings a held-input hero to near-full speed within a few frames, so a distance-only bound sits too close to the buggy value, and a no-input second window moves zero either way. Replaced with a GEOMETRIC discriminator — a second wall at SECOND_WALL_X = 2, one tile past where the first grant lands. phase() ignores colliders by design, so a wrongly re-armed grant drives THROUGH the wall while a guarded controller falls back to walking and collides with it: a binary crossed/didn't-cross fact, independent of the acceleration curve.
  Re-reviewer REPRODUCED THE MUTATION HIMSELF rather than trusting the report: guard deleted -> "expected 3.999999999999999 to be less than 2"; guard restored -> 6/6 green; working tree verified clean both by git status and git diff afterwards. No production code touched; the added toBeCloseTo(3, 6) is a strengthening, not a weakening.
Task 12: complete (commits a700ad04..96c3699f, review clean after fix)
Task 13: implemented (commit 799450c0) — NOT YET REVIEWED. *** RESUME POINT: package 96c3699f..799450c0 and dispatch the task reviewer, THEN Task 14 (the displacement staleness handshake), THEN the whole-branch review. ***
  CONTROLLER VERIFIED INDEPENDENTLY: npm run typecheck:renderer 0 errors; test:renderer 23 files / 184 tests green. The IDE's six diagnostics on hd2d-remote-state.test.ts (playerActorView "not exported", ActorView.y "does not exist") are the DOCUMENTED tsserver misprojection — neither tsc nor vitest reproduces any of them.
  Implementer reports npm run v fully green (224 files / 1960 tests, lint, typecheck, migrations drift, catalog, map check, build) and npm run adventure:proving stamping a 72x72 heightfield. Browser pass done on a FRESH scratchpad DB; the owner's stale dev DB was never touched.
Task 13: CONCERNS RAISED (for the review and the whole-branch review to judge) —
  (1) NO BREATH SIGNAL AT ALL: a player's first and only warning of drowning is drowning. __lindocara.self() returns a PlayerSnapshot, which carries no breath field, so even a dev cannot watch it fall. Ruling 5 stood (verified by outcome: the hero drowned mid-swim, body left in place) but this is a real UX gap now that drowning kills.
  (2) stepHero's HeroEvent[] still plays NOTHING — footsteps, splashes, water entry/exit, canopy, skids all narrated and silent. Audio was ruled OUT OF SCOPE here (ruling 4); it now has no owner at all.
  (3) NO VERTICAL ANIMATION — no stretch/squash (the lab drives those from state.vy, which never crosses the wire) and NO CANOPY SPRITE over a glider: `gliding` moves the body and draws nothing. Recorded in docs/hd2d-rendering.md by the implementer.
  (4) PRE-EXISTING falsehood left alone on scope grounds: packages/server/AGENTS.md:7 still says production is "Cloudflare Workers + Durable Objects + D1", which root AGENTS.md explicitly contradicts (Bay; the Worker is frozen). The Bay move falsified it, not this increment.
  (5) THE TASK 9-12 DOC SWEEP WAS INCOMPLETE — packages/server/AGENTS.md still listed "12 queued commands" as a live cap after the command queue was deleted. Fixed here, but the earlier tasks missed that file.
Task 13: review: NEEDS FIXES — 0 Critical, 2 Important. Reviewed on opus. Rendering half APPROVED; the DOC half is what failed.
  Imp 1: root AGENTS.md's src/shared/ file map now declares simulation.ts DELETED and removed its map entry. IT IS NOT DELETED — it still exports TICK_HZ/TICK_MS/TICK_DT/NETWORK_SNAPSHOT_HZ/PLAYER_SIZE/WORLD_*/clampToWorld/Input/NO_INPUT, and packages/client/src/game/net.ts:39 imports TICK_MS and Input FROM IT, in the very file this same commit documents as the move report's home. Only prediction.ts is gone. The commit CONTRADICTS ITSELF: packages/engine/AGENTS.md, edited in the same diff, correctly describes simulation.ts as the surviving clock + Input. A new falsehood in the repo's most-read file is worse than the stale sentence it replaced.
  Imp 2: packages/server/AGENTS.md:7 still presents "Cloudflare Workers + Durable Objects + D1 in production" INSIDE a sentence this diff rewrote — while root AGENTS.md's Bay section says the retired Worker "must never be presented as the live application". The implementer disclosed it and argued the Bay move (not this increment) falsified it; ruling 2 said correct what is FALSE in the package guides, and its hands were already in that sentence.
  VERIFIED GOOD (reviewer checked against source, not the report): ActorView's y + three flags and the elevationOf placement; elevationOf reads a GROUNDED actor from the terrain rather than the reported y (the non-obvious half — a stale grounded elevation would jitter the sprite through its own floor); the new test DISCRIMINATES IN BOTH DIRECTIONS (reverting to ground-snapping fails tests 1/2/3, the inverse always-read-y error fails test 4, dropping a flag at the seam fails test 5); the exported playerActorView seam is the right place to spend an export (a flag dropped in #collectActors would leave every placement test green); the doc ARITHMETIC re-derived from source — SPATIAL_CELL_SIZE 256/TILE_SIZE, the six AOI radii, 96/TILE_SIZE hysteresis, WORLD_POSITION_DELTA_THRESHOLD 0.5/64, targetMoveThreshold 72/64, MOVE_REPORT_MS = TICK_MS, NETWORK_SNAPSHOT_HZ 10, isDirection's unit-length requirement; the death-section rewrite accurate on all four claims (speedForLife folded in before the step, corpse fed a ZEROED input rather than skipped so gravity/water/thin-ice keep running, applyReportedMove refusing corpse/transitioning/disconnecting frames, withinRoomBounds).
  BROWSER PASS judged SHOWN, NOT CLAIMED: landed y 0.90 is exactly the fixture's levelHeight 0.9; the glide samples 1.02 -> 0.58 -> 0.14 are a near-constant descent rather than an accelerating one; the remote arc is 24 CONSECUTIVE samples, not one lucky frame. Screenshots are in a scratchpad and both browsers were closed, so the traces can only be taken on their face.
Task 13: minor (deferred): playerActorView's docblock is ORPHANED onto GROUNDED in game-renderer.ts (nearest-preceding-comment wins, so the exported seam has none on hover and GROUNDED carries the wrong one) — the same shape exists pre-existing above facingOf. ~70 lines of fixture (mapOf/flatMap/sceneFor/textureRegistryOf/meshes/drawnElevation) duplicated from hd2d-billboards.test.ts, so the two drawnElevation foot-offset computations must now be kept in step by hand. docs/directional-action-combat.md's added bullet says combat facing "reste sa decision, pas celle du client" — the server freezes a CLIENT-SUPPLIED vector at wind-up, so the TIMING is the server's decision, the VECTOR is not.
Task 13: fix round 1/5 (2 Important + 1 folded-in Minor addressed, 0 open; commit 5c284411).
  Re-reviewer verdicted on TRUTH, not on presence of an edit: read simulation.ts himself (TICK_HZ/TICK_MS/TICK_DT/NETWORK_SNAPSHOT_HZ/NETWORK_TICKS_PER_SNAPSHOT/NETWORK_SNAPSHOT_MS/WORLD_WIDTH/WORLD_HEIGHT/PLAYER_SIZE/clampToWorld/Vec2/WorldBounds/Input/NO_INPUT survive; step()/PLAYER_SPEED gone, prediction.ts gone) and confirmed the new file-map line says exactly that and no longer contradicts packages/engine/AGENTS.md. Grepped packages/server/AGENTS.md whole: no other sentence asserts Cloudflare as the LIVE runtime (the D1 mentions are portability discipline, matching root AGENTS.md's own section).
  Minor 4 mutation REPRODUCED INDEPENDENTLY: branches swapped -> "expected 12 to be close to -0.6" on the new desynced-flags test alone, the other 5 green; restored -> 6/6; working tree verified empty by git status AND git diff.
  SWEEP CLAIM SPOT-CHECKED (the fixer claimed "nothing else false" across every doc hunk in 799450c0): re-reviewer picked three cheap-to-falsify assertions and verified all three against source — MOVE_REPORT_MS = TICK_MS at net.ts:203 literally; DIALOGUE_CLOSE_RADIUS = 3 at event-commands.ts:72; and all eight interest.ts figures (cell 4, 14.0625, 13.28125, 10.15625, 14.0625, 13.28125, 10.9375, hysteresis 1.5). Claim holds on the sample.
Task 13: complete (commits 96c3699f..5c284411, review clean after fix)
Task 13: minor (deferred): packages/server/AGENTS.md's "Volatile-state caveats" still says "a CF isolate eviction loses it" — PRE-EXISTING (confirmed by git log -S), outside both this fix's scope and the sweep's target commit. Carry to the whole-branch review with the pre-existing Cloudflare line at packages/server/AGENTS.md:7's neighbours.
*** ALL 13 PLANNED TASKS COMPLETE. Task 14 (controller-authored, author-requested) NOW RUNNING: the displacement staleness handshake. Then the WHOLE-BRANCH review, whose scope must ALSO absorb the skipped scoped re-review of Task 11's fix (22fa4fbd..a700ad04). ***
Task 14: brief WRITTEN (task-14-brief.md) but NOT DISPATCHED — the author interrupted the implementer dispatch. The brief stands ready: prove the bug first (Step 1 is a BLOCKED-if-it-does-not-reproduce gate), then ONE displacement helper rather than fourteen seq++ sites (the 14-site edit is the same trap as Task 7's two .x-without-.z Criticals, with a second face), the counter on the wire both ways with parseMove's absent-key rule, and the drop beside withinRoomBounds. 13 server displacement sites enumerated in it by grep. NOT part of the whole-branch review's scope.
*** WHOLE-BRANCH REVIEW NOW RUNNING — base 9cc7337f (the increment's first parent) .. HEAD. Its scope ABSORBS the skipped scoped re-review of Task 11's fix (22fa4fbd..a700ad04), which was verified mechanically (typecheck 0, 1941 tests) but never judged. ***
*** WHOLE-BRANCH REVIEW COMPLETE (opus, base 9cc7337f..5c284411): 1 CRITICAL, 4 Important, 9 Minor. Verdict: READY TO MERGE WITH FIXES. ***
  Method: read the FINAL STATE of the load-bearing files in a DETACHED WORKTREE (the main checkout is dirty with the author's own in-progress Task 14 work — the reviewer caught that itself and refused to review unreviewed future work as if it were HEAD); ran server 547 / client 217 / engine 871; applied and reverted 21 MUTATION PROBES; delegated a full unit-system sweep and a full axis-pairing sweep. Worktree removed and pruned; main checkout verified byte-identical afterwards.
  *** CRITICAL: packages/engine/src/protocol.ts:1067 — `value.width >= 1` is a surviving PIXEL floor in a wire validator, and it KILLS EVERY SACRED PASSAGE TRAIL FRAME. The emitted width is always below 1: talents.ts:712 authors 22/TILE_SIZE = 0.34375 and priest-variant-system.ts:158 floors it at 1/TILE_SIZE = 0.015625 (with a comment above explaining that one tile would be wrong here). PROVEN, not reasoned: feeding the exact frame worldTick.ts:2512 emits to parseServerMessage returns null. Consequence is not a missing sparkle — net.ts:302-315 treats an unparseable frame as CORRUPTION and burns a #requestResync (1/s), and sendPriestLumenEffectsTo (worldTick.ts:643) replays trails ON ADMISSION, so a player joining a room with a live trail takes the same path. net.ts:676's onLumenTrail handler is unreachable. WHY NOTHING CAUGHT IT: protocol.test.ts:1126 still asserts on the PIXEL fixture width: 22, which passes >= 1 — the test and the runtime disagree about units, so the bound is green in CI and dead in the game. This is the REWARD_DISTANCE = 900 defect class, SECOND INSTANCE. Fix: `value.width > 0 && value.width <= 64`, retire the pixel fixture, and add a round-trip test that feeds a SERVER-SHAPED frame through the parser. *** ALSO PRESENT IN THE AUTHOR'S WORKING TREE (at protocol.ts:1104 there) — it needs fixing whichever lands first. ***
  Imp 2: THE MOBILITY GRANT'S SPEND IS PINNED BY NOTHING, on either side of the wire — the grant is one of only TWO sanctioned client decisions. Three mutations all UNCAUGHT: worldTick.ts:1033 debitHeldMobility made a no-op (its own docblock calls it load-bearing); hero-controller.ts:245 dropping `grant.remaining > 0` (client phases past its granted distance); hero-controller.ts:269 dropping `grant.window <= 0` (the grant never expires). The ARMING rules are well covered; it is the SPEND that is unpinned.
  Imp 3: worldTick.ts:983 — deleting the whole life/handoff fence (`life === "corpse" || transitioning || disconnecting`) leaves 547/547 SERVER TESTS GREEN. This is the fence root AGENTS.md names explicitly ("a body that reports itself walking is not believed"). The client-side frozen flag IS tested; the server half — the actual fence — is not.
  Imp 4: packages/engine/src/game.ts:236-243 — the comment claims authored monster tuning "is converted at the map boundary with the rest of the authored geometry". THERE IS NO SUCH CONVERSION: authored-monster-system.ts:49 passes event.monsterSpeed through RAW, while its immediate neighbour patrolRadius really does convert via authoredPatrolRadius. A stored pixel speed of 105 would spawn a monster at 105 TILES/second. Inert ONLY because the events: [] bake means no authored monster spawns — so it FIRES THE DAY THAT COST IS CLOSED, which is the next planned work. The comment actively prevents the next reader from finding it.
  Imp 5: packages/engine/test/ground-distance-units.test.ts — the branch's OWN anti-regression guard has two defects. (a) `LARGEST_GRID = 64` with a comment asserting 64 is the largest heightfield an author can build; it is 256 (hd2d/map-data.ts:42). Conservative today, but anyone "correcting" it to 256 silently weakens the guard fourfold. (b) line 103 `expect(`${name}=${value}`).toBe(`${name}=${value}`)` is a TAUTOLOGY that always passes — it was meant to surface which constant failed and cannot. It also did not catch the Critical, because that literal lives in a WIRE VALIDATOR rather than the named-constant list: a guard over protocol.ts's numeric bounds is the missing sibling.
  Minors: worldTick.ts:1026 vs snapshot-system.ts:45-52 (one concept, two predicates — inert, only blink has shape "teleport"); worldTick.ts:1055 applyDrowning's life guard untested (547/547 green without it) — it is the only thing refusing the ledger's own floating-corpse re-drown case; terrain-access.ts:182 canStandOrEscape's "no worse relief" clause untested (`if (false)` leaves server+client+engine all green) — a regression would let a stuck body escape UPWARD through a cliff; hero-controller.ts:283 teleport() cancelling an in-flight grant and :200-202 place() clearing swimming, both survive mutation; monster-system.ts:327 keeps the REAL hero's y on a decoy target (inert — startMonsterAttack reads only x/z — but it is the axis trap in its READ form); WorldRoom.ts:1862-1871 still ships pixel {x,y} cemeteries/questNpc/portals on every welcome (confirmed NO reader exists); protocol.ts:1109/:1151 radius <= 512 rejects nothing (harmless direction); docs/directional-action-combat.md's facing bullet (the server freezes a CLIENT-SUPPLIED vector — timing is the server's, the vector is not); packages/server/AGENTS.md's "a CF isolate eviction loses it".
  *** TASK 11'S FIX (22fa4fbd..a700ad04) — JUDGED AT LAST, VERDICT: SOUND. *** All four claims verified and BOTH new tests proven discriminating: restoring the y clause makes the elevation test FAIL (so it pins behaviour, not decoration); restoring the #reportedAt reset makes the throttle test fail with "expected 56 to be less than or equal to 20" — a decisive margin, and the docblock's ~30/s estimate was conservative. The reviewer also INDEPENDENTLY enumerated the server displacement sites and confirmed the premise: every one moves the hero across the GROUND, none moves y alone. The editor's inlined constant under a banner is the right call over a live import to a deleted file. The throttle test deliberately drives displacement FASTER than any real snapshot stream and says so, because what it pins is that the throttle is UNCONDITIONAL — the right way to write that assertion.
  LEDGER CORRECTIONS from the sweeps — three items this ledger still calls open are ALREADY RESOLVED: Task 3's clampRestoredPosition/profileFromAttachment AND loot-system.ts's pointDistance (AdmissionService.ts:151/155/290 re-ground correctly, restoreStandablePosition returns all three axes, loot-system clean, LootSnapshot carries z); Task 4's unconverted monster-system.test.ts (green at HEAD); Task 5's fourth hasPeasantHarvestLineOfSight caller (world-room-events.test.ts green at HEAD).
  ALSO CLOSED BY THE SWEEP: Task 3's MAX_STEP second copy is substantially closed — hero-controller.ts:170 passes the ENGINE constant, so the shipping game has one source; only apps/lab/settings.ts:34 keeps a literal, and the lab is a witness. The epsilons agree numerically (terrain-access.ts:77 and hero-step.ts:51 both 1e-3).
  AXIS-PAIRING SWEEP CLEAN: all 84 direct axis writes pair x+z with y as elevation; Object.assign / ["x"] / [axis] / .position = forms return ZERO hits repo-wide in the affected packages; 6 spread-with-override sites, 5 correct (the 6th is monster-system.ts:327 above). After two Criticals of exactly this shape earlier on the branch, the final tree does not carry a third.
*** FIX WAVE BLOCKED ON THE AUTHOR — the working tree carries the author's OWN in-progress Task 14 work (27 modified files + untracked world-room-displacement.test.ts) in exactly the files most findings live in: protocol.ts, protocol.test.ts, worldTick.ts, skill-system.ts, snapshot-system.ts, world-runtime.ts, net.ts. A fix wave committing those paths would sweep half-finished Task 14 work into a review-fix commit. DO NOT dispatch one until the author says how to sequence it. ***
FINAL FIX WAVE (commit 3d864514, scoped to files the author is NOT touching): all 5 dispatched findings ADDRESSED. Re-reviewed on opus.
  Finding 1 — THE FIXER REFUSED THE FIX IT WAS ASKED FOR, AND WAS RIGHT. It implemented authoredMonsterSpeed() mirroring authoredPatrolRadius, then traced the data flow and reverted it. The re-reviewer INDEPENDENTLY CONFIRMED the double-division claim is TRUE: MONSTER_STATS speeds are ALREADY tile-scaled (goblin 105/TILE_SIZE), defaultMonsterTuning returns that fraction, and map-events.ts:743-747's boundedMonsterSpeed FALLS BACK TO IT — whereas patrolRadius (map-events.ts:695-698) has NO fallback and is always explicit pixels. That asymmetry is what makes authoredPatrolRadius a total conversion and a monsterSpeed mirror an untotal one. boundedMonsterSpeed's own docblock declares the field TILES per second, so a blanket /TILE_SIZE at the consumer contradicts the producer's contract; the editor (EventDialog.tsx:151/:475, editor-state.ts:610/625/678) prefills and commits the tile-scaled default unrescaled. A goblin's stored value is ~1.64; the mirror would have made it 0.0256 — 64x too slow for EVERY monster, to rescue one hypothetical legacy row. authored-monster-system.ts verified byte-identical. The replacement comment's every load-bearing claim was checked and holds.
  THE UNDERLYING DEFECT IS STILL LIVE and correctly deferred, with firing conditions now precise: mapEvents.ts:48-54 records that migration 20260805034500_monster_speed_real CONVERTED NO VALUES, so any pre-conversion row is still a raw pixel integer; MONSTER_TUNING_LIMITS.speed.max = 300 admits it; it reaches MonsterSpawn.speed untouched. The five parked adventures store monsterSpeed: null and are SAFE — the exposure is hand-edited pre-conversion maps in the PRODUCTION DB, and it fires the moment authored content lands in heightfield coordinates. Real home for the fix: map-events.ts, where the fallback lives.
  Finding 2 ADDRESSED with a caveat the re-reviewer PROVED: the bound is genuinely derived (imports MAX_HEIGHTFIELD_SIZE, not a hand-copied literal) and the tautology is gone (mutating REWARD_DISTANCE back to 900 now fails TWO assertions with the constant NAMED in the it.each title). BUT loosening the catch-all 64 -> 256 WEAKENS IT 4x: mutating MONSTER_AGGRO_RANGE to 210 now produces ONLY the exact-pin failure — the "inside a grid" assertion PASSES because 210 <= 256, where the old bound caught it. Every pixel original <= 256 (210, 192, 96, 92, 54, 46, 44, 42) is now invisible to the catch-all, INCLUDING 3*TILE_SIZE = 192, the very DIALOGUE_CLOSE_RADIUS bug this file was written about. The fix is factually right (256 IS the real bound); the trap is just coarser.
  *** CONTROLLER ADJUDICATION (residual, PARKED — no second fix wave): the catch-all's lost sensitivity is REAL but NOT load-bearing. Every constant in the list is ALSO exactly pinned by two toBe assertions, so the catch-all only guards a FUTURE constant whose pixel original nobody knows. Ruling: park with the re-reviewer's own remedy recorded — add a SECOND, tighter assertion beside the true MAX_HEIGHTFIELD_SIZE bound (every ground constant < 64, or < the largest pixel original / TILE_SIZE). Do this when the wire-bounds sibling guard is written, since the Critical showed both are missing for the same reason. ***
  Findings 3/4/5 ADDRESSED: targetPosition verified to genuinely carry an elevation (RogueSilhouetteRuntime and RangerAfterimageRuntime both extend WorldPosition and are constructed with a real y at worldTick.ts:3769-3771 and :3952/:3957), so `y: targetPosition.y` introduces no new bug — currently inert hygiene, since startMonsterAttack reads only x/z. The French correction is idiomatic and now states the boundary exactly (the freeze's MOMENT is the server's, the vector is not), matching worldTick.ts:1043 storing move.facing verbatim. The Bay/Node correction is consistent with root AGENTS.md.
  Scope discipline CONFIRMED: exactly the five permitted files; every off-limits path untouched; the author's dirty set identical before and after (27 modified + the untracked world-room-displacement.test.ts), and both files the re-reviewer mutated for its proofs show an empty git diff.
  Deferred (from the re-review): game.ts:236/:249 say MONSTER_STATS.speed is "above" when it is ~700 lines below; packages/server/AGENTS.md's restart sentence is a zeugma ("Node sweeps idle rooms after 5 minutes, and so does a restart" — a restart does not sweep after 5 minutes); ground-distance-units.test.ts:79-80's comment has a dropped word; MONSTER_TUNING_LIMITS.speed.max = 300 is now a bound on a tiles-per-second field where the whole bestiary sits under 2.0 — it admits three orders of magnitude of nonsense and is the natural parse-time gate to refuse a legacy pixel row once provenance is settled.
*** INCREMENT COMPLETE THROUGH THE WHOLE-BRANCH REVIEW AND ITS ONE FIX WAVE. WORKSPACE DELIBERATELY NOT DELETED: it holds task-14-brief.md (in flight, uncommitted, in the author's tree) and the record of the OPEN CRITICAL (protocol.ts trail-width pixel floor) plus two Important test gaps that could not be fixed without touching the author's files. NOT PUSHED — that is the author's call. ***
*** COMMITTED + PUSHED (author instruction): the author's own Task 14 displacement-stamp work landed as 5597394a (28 files) and main..origin/main is pushed through it. CI deploys from it. One flaky run on the way in (2 tests in an auth/fetch-mock file) passed clean on re-run; 225 files / 1983 tests green. ***
Task 15 (controller-authored, author-approved): THE HEIGHTFIELD SEEDING ENDPOINT. Implemented (commit b65051e0), reviewed on opus.
  Route: PUT /api/maps/:id/heightfield, use [$secure({}), $transactional()], loose schema, body {heightfield: "<encodeMap output>"} -> 204; 400 map_invalid, 401 anonymous, 404 map_not_found (missing OR non-owner, byte-identical message), 413 over 4 MiB; revision bumps on every write.
  *** SECURITY VERDICT: THE FENCE IS SOUND, PROVEN THREE WAYS BY THE REVIEWER, NOT READ. *** (1) the unfenced writer is unreachable over HTTP — the only non-test callers are build-proving-map.ts:256 and the controller, which calls saveHeightfieldForUser, and MapService.updateMap writes an EXPLICIT FIELD LIST with no heightfield key, so the collaborative PUT cannot set it even with a crafted body; (2) the identity is session-resolved (user.id from $secure({})), nothing in parseHeightfieldBody reads an id; (3) LIVE END-TO-END against the real dev app: anonymous PUT -> 401, foreign account PUT -> 404 map_not_found, map afterwards revision 4 / heightfield 67816 bytes UNCHANGED. Both required mutations reproduced independently (fence weakened -> "expected 204 to be 404"; decode gate weakened -> 2 failures), each restored with an empty git diff.
  Validate-then-store operates on the SAME BYTES (mapAuthoring.ts:266-269 returns the very string it fed to decodeMap — no re-encode, no second parse), so the classic validate-then-transform bypass is absent by construction. The non-owner test asserts the ROW (same heightfield AND same revision), which is what catches a route that refuses AFTER writing. The 401 test deliberately sends a body a tight schema would 400, so it actually proves the schema-before-$secure ordering the loose schema exists to protect.
  DEVIATION, JUDGED CORRECT: the ownership idiom was copied from PartyService.deleteParty, NOT from the map siblings — because the map siblings enforce NOTHING by ported design. The reviewer re-derived that independently and confirmed it was the only fence in the package to copy.
  Task 15 review: 0 Critical in the diff, 3 Important.
    Imp 1: NO HEIGHTFIELD-SPECIFIC SIZE BOUND. MAX_MAP_JSON_BYTES (4 MiB) is documented in bodySizeCap.ts:31-38 as EQUAL TO THE GLOBAL PARSER CEILING — a documented no-op — and decodeMap bounds only `size` (<=256), leaving colliders/spawns/elements/events UNBOUNDED arrays. PROBE: a payload of size 1 padded with 161,161 colliders, 4,190,326 bytes, ACCEPTED by decodeMap in 24 ms. A legitimate 256x256 heightfield is ~1 MB, so the ceiling is ~4x the honest maximum; AdventureService caps maps per adventure (16) but NOTHING caps adventures per account, and the stored string is re-sent VERBATIM in every welcome.
    Imp 2: the oversize-body test DOES NOT DISCRIMINATE — removing enforceBodySizeCap from the handler entirely leaves 12/12 green, because Alepha's global parser answers 413 first.
    Imp 3: MapService.ts:457-459's docblock — the argument for KEEPING an unfenced writer — names seed-proving-adventure.ts's local path as an in-process caller, and THIS SAME COMMIT moved that script onto HTTP. The defence rests on a caller that no longer exists.
  Also noted: the "ownership as invisibility" claim in MapService.ts:492-493 OVERREACHES — GET /api/maps/:id is unfenced and returns the full payload to any account, so the 404 hides nothing (right for consistency, wrong in its stated reason).
*** PRE-EXISTING CRITICAL, NOT CAUSED BY THIS DIFF, SURFACED FOR THE AUTHOR'S DECISION: ANY AUTHENTICATED ACCOUNT CAN READ, REWRITE, RE-FLAG AND **DESTROY** ANOTHER AUTHOR'S MAPS. Executed, not read — the existing suite already asserts it (maps.test.ts:794 "a foreign account may list, read and edit any adventure's maps", passes in isolation with the rival's PUT returning 200), and the reviewer's own throwaway real-HTTP probe as a freshly registered second account got: LIST 200 (the owner's map ids), GET 200 (full payload), POST /maps/:id/first 204 (flips the owner's front-door map), DELETE /maps/:id?force=true 204 -> THE MAP IS GONE. This is the ported "collaborative editing is open" behaviour, asserted by a test written on purpose. It bounds the new fence's practical protection: an attacker cannot blank your terrain IN PLACE, but can DELETE THE MAP THAT HOLDS IT. The author must decide whether the map surface stays collaborative before more of it is publicly reachable. ***
Task 15: fix round 1/5 (commit 06f70f56) — Imp 2 and Imp 3 ADDRESSED, Imp 1 NOT ADDRESSED. The re-reviewer probed rather than read, and found the fix's byte half does not survive one request header.
  Imp 2 ADDRESSED, mutation-proven independently: replacing enforceBodySizeCap with `void headers;` -> "expected 400 to be 413" at map-heightfield.test.ts:327; disabling the count bound -> "expected 204 to be 400". Both restored, 13/13.
  Imp 3 ADDRESSED: grep confirms exactly ONE non-test production caller of the unfenced writer remains (build-proving-map.ts:256); seed-proving-adventure.ts no longer references MapService at all and rejects --database. The docblock names precisely that set.
  Imp 1 — WHAT HOLDS: the COUNT bound genuinely bites (size 1 + 50,000 colliders, 1.5 MB, under both caps -> 400 map_size, stored null) and no honest terrain is refused (the real 72x72 proving heightfield 67,816 B -> 204; a maximal honest 256^2 grid 1,179,815 B -> 204; 256^2 with 6,553 colliders + 6,553 elements -> 204).
  *** Imp 1 — WHAT DOES NOT: TWO HOLES, BOTH PROVEN BY REAL HTTP. ***
    (a) THE COUNT BOUND CONSTRAINS HOW MANY ENTRIES, NEVER HOW BIG. HeightfieldElement.assetId, spawns[].name and HeightfieldEvent.id/graphicAssetId are UNBOUNDED STRINGS in decodeMap. A size: 32 grid satisfying the count bound EXACTLY, padded through element assetIds: 1,983,653 B accepted and stored. A 1x1 GRID WITH ONE ELEMENT: 2,093,257 B accepted and stored — 1.8x the largest honest 256^2 map, from a map with a single cell. So the count bound is never what binds; the byte cap always is.
    (b) THE BYTE CAP IS GZIP-BYPASSABLE. enforceBodySizeCap reads content-length, which for a compressed request is the COMPRESSED size, while bodyParserOptions.inflate is true and Alepha inflates BEFORE the handler runs. PROVEN: Content-Encoding: gzip, request body 8,368 bytes -> 204, STORED 8,388,809 bytes (8.0 MiB) — 4x this route's own cap, 2x the global ceiling, and 2x what the original finding's probe achieved. The only remaining ceiling is decompressBuffer's limit * 10 = 40 MiB. Chunked (no content-length) is NOT a bypass — the JSON.stringify fallback caught a 3,145,985 B streamed body with 413.
    AMPLIFICATION: the stored string is re-sent VERBATIM in every welcome, and isWorldInfo (protocol.ts:1732) only requires that it DECODES — no length bound on the wire. So ONE 65 KB REQUEST BUYS A 40 MiB WELCOME FRAME FOR EVERY JOINING CLIENT, and N such writes buy N x 40 MiB of database on a single-process host. The route is owner-fenced, so the victim map is the attacker's own — but any account can register and create one.
    THE VECTOR IS PRE-EXISTING (enforceBodySizeCap behaved this way at b65051e0 and on EVERY other map route) — but the finding under review is "this route has no effective size bound", and a bound removable by one header does not close it.
    FIX: bound heightfield.length INSIDE parseHeightfieldBody — measure what you STORE, not a header describing the wire. Closes gzip, deflate and br at once, no framework change.
  Deferred (re-reviewer): map_size maps to editor.error.size in client/src/api.ts:369, whose prose is "Map size must be between 20x15 and 256x256 tiles" — wrong sentence for "more entries than cells" if the editor ever calls this route. The <= size^2 bound is generous globally but the editor's element model allows 16 decorations per cell, so a small densely-decorated grid could plausibly exceed it once heightfield authoring lands. WorldInfo.heightfield has NO length bound on the wire, so the write-side cap is the ONLY thing between a stored string and every client's welcome. ServerBodyParserProvider's content-length-only pre-check plus inflate: true makes EVERY enforceBodySizeCap call site in this app compression-bypassable — an upstream per-action bodyLimit (already noted in bodySizeCap.ts as a feature request) would fix the class, and that is a VENDOR/UPSTREAM candidate for the ../alepha dogfood loop.
Task 15: fix round 2/5 (commit e7696d40) — Imp 1 ADDRESSED. MAX_HEIGHTFIELD_BYTES = 1.5 MiB, measured in parseHeightfieldBody with TextEncoder().encode(heightfield).length ON THE DECODED STRING, before decodeMap — not on a header. Answers 413 request_too_large, deliberately the SAME answer an uncompressed body of that size already got, so gzipping a request does not change what the caller is told. The count bound keeps its 400 map_size.
  RE-REVIEWER PROBED ALL THREE ENCODINGS OVER REAL HTTP, and the column-unchanged check is against a SENTINEL written immediately before each attempt, so it proves a non-write rather than an absence: gzip 2,255 B wire / 2,097,355 B inflated -> 413; deflate 2,243 B -> 413; **br 155 B ON THE WIRE, a 1024x ratio -> 413**; the exact round-1 attack (8,368 B -> 8,388,807 B) -> 413 with the proving map still 67,816 B; 32 MiB -> 413 in 62 ms; 48 MiB -> the framework's own decompress ceiling.
  NO WAY FOUND TO EXCEED THE BOUND IN THE COLUMN: parseHeightfieldBody destructures heightfield ONCE into a const, measures THAT const and returns THAT const, so there is no re-encode and no TOCTOU via a getter. Boundary exact: 1,572,864 B -> 204 stored; 1,572,865 B -> 413 unchanged. No second HTTP write path (updateMap's field list has no heightfield key, and a PUT /api/maps/:id carrying one leaves the column unchanged — probed). UTF-8 MEASUREMENT MATCHES STORAGE WITH NO GAP: 380,000 astral chars measured 1,520,203 utf8 (760,203 utf16), accepted, column holds exactly 1,520,203; lone surrogates count 3 B each and Buffer.byteLength agrees. TextEncoder never UNDER-counts what SQLite stores. A code-unit .length would have been the wrong measure and the fixer used bytes.
  MARGIN VERIFIED BY MEASUREMENT, not arithmetic on trust: worst honest 256^2 = 1,179,817 B, bound 1,572,864 B, headroom 393,047 B. Marginal cost per rich element measured flat at 76.7 B across 3,000/5,000/5,500/6,000-element payloads -> ~5,100 entries (report said 5,500 — right to its own rounding). Comfortable for two reasons: the 1.18 MB baseline is an all-null-levels grid, i.e. entirely water and unplayable (a real 256^2 with numeric levels encodes ~720 KB, leaving ~11,000 elements), and the project's own tile format caps elements at MAX_MAP_ELEMENTS = 2,000. Honest terrain confirmed passing: the actual buildProvingMap() output (67,816 B) -> 204, maximal honest 256^2 (1,179,817 B) -> 204 stored byte-identical.
  Mutation reproduced independently: guard -> `if (false)` gives EXACTLY ONE failure, "expected 204 to be 413", and it fails on the attack LANDING (the 2 MB payload stored), not on an incidental assertion. Restored from a byte backup, shasum matched, 15/15.
  413/400 SPLIT JUDGED DEFENSIBLE: both codes are mapped client-side (api.ts:369 -> editor.error.size, :380 -> editor.error.too_large) and both dictionary keys exist in FR and EN.
  Local seeding path VERIFIED WORKING against an isolated scratchpad DB on port 5199 (the owner's dev DB mtime confirmed untouched): "Stamped 72x72 heightfield on map 0ebba1bd... - 1192 ground, 3992 water, 48 elements, 1 events".
  *** SAFE TO DEPLOY: YES. ***
Task 15: complete (commits 5597394a..e7696d40, review clean after 2 fix rounds)
Task 15: minor (deferred): the framework compression bypass survives on this route as a PARSE-COST surface, not a storage one — a ~33 KB gzip body still costs a 32 MiB inflate + JSON.parse (~62 ms) before the 1.5 MiB check refuses it, with the framework's ceiling at 40 MiB; carry the MEASURED 1024x amplification ratio into the upstream dogfood item. MAX_HEIGHTFIELD_JSON_BYTES (2 MiB) is now strictly wider than the real bound (1.5 MiB) and fires only for uncompressed bodies — two constants in different files with different derivations, inviting the next reader to move one without the other. map_size maps to editor.error.size whose prose is about grid dimensions, not entry counts.
