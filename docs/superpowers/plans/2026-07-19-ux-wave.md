# UX Feedback Wave Implementation Plan (tranche 4.5)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Requirements:** `docs/superpowers/specs/2026-07-19-ux-feedback-wave.md` — the user's 13 remarks, verbatim, binding. **Exit gate:** a long real-browser Playwright campaign — build and play online adventures, multi-map, multi-player, from scratch, without a hitch.

## Global Constraints

All prior plans' constraints hold. POC: migrations may drop content where the spec says so, but prefer mechanical transforms when cheap. Every task ends with full `npm run check` green + mutation proofs reported.

---

### Task 1: Maps belong to one adventure (#5) + the 5×5 template (#7)

Schema: `map.adventure_id` NOT NULL fk cascade; `adventure_map` table DROPPED; a map is created inside an adventure and never referenced by another. `adventures.ts`/`maps.ts`/HTTP: map create takes the adventure id (ownership checked through it); the graph references its own maps only (validation simplifies); map list is per-adventure. Migration: attribute each map to the single adventure referencing it via `adventure_map`; maps referenced by several → duplicate rows per adventure (cheap, correct); orphan maps → dropped (POC, note it). Client `api.ts` follows. New-map template (#7): every new map is created 5×5 land in the centre of a `MAP_MIN_COLS×MAP_MIN_ROWS` water field, spawn dead centre, via `layersFromBlocks`-style shared helper + the autotile resolver — the server builds it (`defaultMapInput(adventureId, name)`), the client stops sending cols/rows (the new-map dialog keeps only the name; sizes come later via a resize tool, out of scope). Tests: ownership enforced (PUT a map through the wrong adventure → 404/403), migration attribution incl. the duplicate case, template collision-verified (spawn walkable, border water solid).

### Task 2: The adventure-first entry flow (#2, #3, #4, #6)

Opening the editor lands on an ADVENTURE picker (list + create; no editor without an adventure). Creating one: one POST that creates the adventure WITH its auto first map (server-side, atomic: adventure + default map + graph start pointing at the map's default entry... the graph needs an entry to point at — the template gains one default entry marker/event at spawn; keep it minimal and consistent with what validation requires) — the explicit save the user asked for is the form's Create button (#4). The Cartes panel gains the starting-map control (#6): a per-adventure start selector (replaces the buried settings-dialog start), persisted through the adventure PUT. AdventureSettingsDialog slims down (title/maxPlayers/delete stay; membership UI DIES — ownership is implicit now; links editing stays until #12 rethinks exits). Tests: no-adventure → picker; create → adventure + map + start exist server-side (one POST, assert atomicity via the response); start selector round-trips; the settings dialog no longer offers membership.

### Task 3: Editor UX pass (#1, #8, #9, #11, #13)

(#8) grid on by default (stage + toolbar state). (#1) light-only: strip dark-theme variants from the editor surfaces; `app.css` tokens forced light for the editor tree (do not touch the game's own look). (#11) exclusive selection: palette selection and marker/event tools are ONE selection state — picking a terrain deselects a marker tool and vice versa (the T2-review dual-highlight, user-confirmed). (#9) hover feedback: with any placement selection active, the hovered cell shows a preview outline (thicker border), and an OPAQUE RED cell background when placement is illegal there (reuse `canPlaceElement`/the tool's own validity — one shared `placementLegalAt(tool, cell)` in editor-state, stage renders it). (#13) minimal catalogue: a curated allowlist — ONE bush, ONE tree, ONE monster species in the palette/monster tool (constant in shared, editor filters through it; existing maps with other assets still render — the allowlist gates AUTHORING only, comment why). Tests: default grid; selection exclusivity; `placementLegalAt` table + the red-cell render decision (pure fn); allowlist filters the palette but not rendering. Mutation proofs each.

### Task 4: Preview performance (#10)

Measure first: instrument the preview sandbox (`map-preview.ts`) — frame time and per-frame allocations; find the cost (suspects: per-frame teardown-rebuild, the editor stage's redraw() on preview ticks, unbatched sprite churn). Fix the top cost; assert the fix with a before/after measurement in the report (no invented numbers — measured in the real browser via the Playwright harness + `window.__lindocara` style hooks if needed). Exit: preview visibly smooth in the recorded browser run.

### Task 5: Markers become typed events (#12)

Mini-design INSIDE the task report before code: `map_event.kind` (`"normal" | "entry" | "exit" | "monster"`) + kind-specific fields reusing the page's slots where sane (monster: species+radius on the event; entry/exit: the id the graph binds — the EVENT id becomes the anchor, adventure graph links reference event uuids; label optional). The marker tools/palette section die; the EV tool grows a kind selector; the event dialog shows kind-specific blocks and hides irrelevant ones (an exit needs no pages/conditions this tranche — keep it simple: entry/exit/monster events are single-page, conditions-disabled until a real need). Server: `zoneFromMap`'s spawn/entry/exit/monster consumption reads events-of-kind instead of markers; `MapMarkers` persistence quarantined (columns stay, ignored — POC note) with a one-shot migration markers→events. Validation moves accordingly (entries walkable etc.). Graph editing (AdventureSettingsDialog links) binds exits/entries by event. Tests: migration produces identical runtime behaviour (same spawn point, same entries/exits reachable, monsters spawn — assert against the real DO like the tranche-1 safety test); editor round-trip per kind; graph binding by event id.

### Task 6: The campaign (exit gate)

Hours of real-browser Playwright against `npm run dev`: script a full authoring+playing session — create account A, build a 2-map adventure (terrain, elevation, stairs, decorations from the minimal set, entries/exits wired, a monster, a conditioned event), save everything; create a party, enter with hero A, walk map 1, take the exit to map 2, verify arrival; account B joins the same party (second browser context), both heroes visible to each other, chat, fight the monster; state flip via the registry-conditioned event where reachable. Rerun the whole script 3× for flakes. Every console error is a finding. Screenshots at each stage; failures become fix-loops in this task. Docs: CLAUDE.md + roadmap updated for the ownership model and typed events.

---

**Final:** whole-branch review (opus), fix wave, reconcile origin, merge, push (auto-deploy), prod smoke.
